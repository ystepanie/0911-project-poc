# 문의 라우팅 자동화 — 구현 체크리스트

> 참고 문서: [업무자동화_PoC_세부구현계획.md](업무자동화_PoC_세부구현계획.md) · [문의게시판_만족도조사_프론트_구현계획.md](문의게시판_만족도조사_프론트_구현계획.md) · [문의라우팅_백엔드_구현계획.md](문의라우팅_백엔드_구현계획.md)
>
> **진행 방식**: 각 단계를 구현한 뒤 사용자 검증을 받고, 승인되면 체크(`[x]`)하고 다음 단계로 진행한다. 체크되지 않은 항목은 아직 검증 전 상태로 간주한다.

---

## Phase 1. 프론트 — 매칭 실패 운영 문구 반영 ✅ (구현·자체 테스트 완료, 사용자 검증 대기)

- [x] 1-1. 매칭 실패 시 문의자 안내 문구를 "관련 팀을 찾지 못했습니다. 담당자가 확인 후 다시 안내드리겠습니다." 형태의 **대기 안내**로 수정 (`js/inquiry.js` 실패 분기)
- [x] 1-2. 실패 사유(`no_team_found` / `low_confidence`)에 따라 화면 문구를 다르게 노출 — "자동으로 찾지 못함" vs "가장 가까운 팀은 찾았지만 확신도 낮음"으로 구분 (⚠️ 검증 필요, 아래 안내 참고)

## Phase 2. 프론트 — 설문 트리거 변경 (관리자 확정 + 접속 시 팝업) ✅ (구현·자체 테스트 완료, 사용자 검증 대기)

- [x] 2-1. 문의 데이터 스키마에 `surveyReady`, `surveyReadyAt` 필드 추가 (`js/api.js`)
- [x] 2-2. 문의 작성 화면 하단에 **데모용 임시 버튼** 추가 — 완료된 문의 중 `surveyReady`를 켜는 용도(관리자 액션 대체)
- [x] 2-3. 문의 제출 시 `contact` 값을 `localStorage`에 `myContact`로 저장
- [x] 2-4. 게시판(`index.html`) 로드 시 `myContact` 기준으로 `surveyReady === true && 미응답`인 문의 조회 → 있으면 **모달 팝업**으로 설문 노출 (`getPendingSurvey`)
- [x] 2-5. 기존 "완료 처리 시뮬레이션" 버튼 클릭 시 `survey.html`로 이동하던 흐름 제거 → 클릭 시 완료 안내 문구만 표시 (⚠️ 검증 필요, 아래 안내 참고)
- [x] 2-6. 설문 제출 UI를 `survey.html` 별도 페이지에서 **`index.html` 내 모달**로 전환 (`survey.html`은 직접 링크 접근 경로로 유지, 미변경)

## Phase 3. 프론트 — 관리자 데모 화면 (최소 기능) ✅ **→ Phase 14에서 완전히 제거되고 문의자용 조회 화면으로 대체됨**

- [x] 3-1. "완료됐지만 설문 미발송" 목록을 보여주는 임시 섹션 추가 (정식 관리자 페이지 아님, 흐름 검증용)
- [x] 3-2. "매칭 실패(미배정) 건" 목록 + 수동 팀 배정 데모 UI 추가 — 🐛 (수정) 실패 사유가 `no_candidate`/`low_confidence` 영문 코드 그대로 노출되던 것을 "관련 팀 없음"/"확신도 낮음"으로 한글 표시하도록 수정 (`admin-demo.js`), `low_confidence`는 LLM 재판단 사유(`matchReason`)도 함께 노출

## Phase 4. 백엔드 — DB 스키마 ✅ (작성 완료, 실제 DB 적용 검증 필요)

- [x] 4-1. PostgreSQL 마이그레이션 작성: `teams`, `inquiries`(candidate_teams, matcher_mode, survey_ready, assigned_manually 등 포함), `surveys`, `match_audit_log` (`backend/migrations/001~004`)
- [x] 4-2. `teams` 시드 데이터 입력 (`inquiry-board/data/teams.json`과 동일 키워드로 시작, `backend/seeds/001_teams.sql`) — ⚠️ `chat_space_id`는 Phase 6 전까지 placeholder 값

> ⚠️ 이 환경에는 PostgreSQL이 설치되어 있지 않아 `npm run migrate` / `npm run seed` 실제 실행 검증은 하지 못했습니다. 실제 DB에 적용해보고 문제가 없는지 확인이 필요합니다.

## Phase 5. 백엔드 — 팀 매칭 파이프라인 (부분 완료: 로직만 검증, DB/API 연동은 미완)

- [x] 5-1. `CandidateFilter` / `LlmReranker` 인터페이스 정의 (`backend/src/matching/`)
- [x] 5-2. `KeywordCandidateFilter` 구현 (상위 3~5개 후보 추출, 0개 시 `no_candidate` 처리) — `backend/src/matching/candidateFilter.js`, `node scripts/test-matching.js`로 DB 없이 결과 확인 완료
- [x] 5-3. `MockLlmReranker` 구현 (실제 LLM 호출 없이 결정적 규칙으로 confidence 산출, `matcher_mode = 'llm_mock'` 기록) — `backend/src/matching/llmReranker.js`
  - 🐛 (수정) confidence 상한을 "키워드 매칭 비율"(`keywordScore`)로 계산하던 방식이, 키워드를 많이 등록해둔 팀일수록 불리해지는 구조적 편향을 만든다는 걸 실사용 중 발견. 예: "법인카드 사용 문의 / 지난달 법인카드 사용한 부분 중 영수증 처리가 안된 게 있습니다" → 회계팀(키워드 7개 중 2개 매칭, 비율 0.29)이 confidence 0.49로 `low_confidence` 실패 처리됐던 걸, **"매칭된 키워드 개수"(`keywordHits`)** 기준으로 상한을 계산하도록 변경 → 동일 문의가 confidence 0.6으로 성공 매칭되는 것 확인 (`candidateFilter.js`에 `keywordHits` 필드 추가, `llmReranker.js` 공식 수정)
- [x] 5-4. `POST /api/inquiries` — 등록 + 매칭 파이프라인 실행 (임시 인메모리 저장소 사용, `backend/src/store/inMemoryStore.js`) + `GET /api/inquiries/:id` 함께 구현, curl로 매칭 성공/실패/검증 오류 케이스 확인 완료 (⚠️ 서버 재시작 시 데이터 유실 — DB 연동 전까지의 임시 상태)

## Phase 6. 백엔드 — Google Chat 연동 ✅ (Mock으로 대체, 실 연동은 후속 승인 필요) → **6-4에서 일부 실 연동 완료**

- [x] 6-1. Google Chat App 등록, 서비스 계정/권한 설정 — ⚠️ 보류: 사내 Workspace 관리자 협업이 필요해 이 환경에서는 불가. 대신 `MockChatClient`로 대체하고, 동일 인터페이스(`sendMessage`)의 `LiveChatClient`로 교체하면 되도록 구조만 마련
- [x] 6-2. 팀별 전용 스페이스 생성 및 `teams.chat_space_id` 매핑 — placeholder 값(`spaces/PLACEHOLDER_*`)으로 매핑, 실제 스페이스 생성 시 교체 필요
- [x] 6-3. 매칭 성공 시 팀 스페이스로 메시지 전송 로직 구현 — `backend/src/chat/mockChatClient.js`, `POST /api/inquiries`에서 매칭 성공 시 호출, `GET /api/mock-chat/log`로 전송 로그 확인 가능 (curl로 성공/실패 케이스 모두 검증 완료)
  - 🐛 (수정) Slack으로 실제 확인해보니 **Chat 메시지에 문의 내용(`content`)이 아예 빠져 있던 버그**를 발견 (제목만 전송되고 있었음). 전체를 다 넣으면 Chat이 지저분해질 수 있어, `messageBuilder.js`에 200자 초과 시 "…(문의 ID로 전체 내용 확인 가능)"로 잘라내는 `previewContent`를 추가해 절충. 짧은 문의/200자 초과 문의 둘 다 curl로 확인 완료.
- [x] 6-4. (추가) **수신 웹훅 기반 실 연동** — `backend/src/chat/liveChatClient.js` + `chatClientRegistry.js`로 팀별 웹훅 유무에 따라 Mock/Live를 자동 분기하도록 구현. 메시지 문구 조립은 `messageBuilder.js`로 Mock/Live 공용화. `backend/.env`(git 미포함, `.env.example`만 커밋)에 `CHAT_WEBHOOK_*` 5개를 채우면 해당 팀부터 실 전송됨. **실제로는 Google Chat이 아니라 Slack 수신 웹훅**으로 5개 팀 전체를 동일 URL에 연결해 검증 — Slack과 Google Chat 웹훅이 둘 다 `{"text": "..."}` JSON POST 계약을 써서 코드 변경 없이 그대로 동작함을 확인. curl로 `chatSentAt` 기록, `chatSendError: null`, mock 로그는 비어있는 것(=실 전송 경로를 탔다는 뜻)까지 확인.
  - ⚠️ 수신 웹훅은 **발신 전용**이라 리액션 이벤트를 받아올 수 없음 — 완료 트리거(Phase 7-1)는 여전히 Mock 페이로드로 처리, 실 이벤트 수신은 Workspace Events API + Pub/Sub 구독이 필요한 별도 작업(후속 과제)
- [x] 6-5. (추가) **완료 트리거를 리액션 대신 "상세 페이지 링크"로 전환** — 6-4의 리액션 수신 불가 한계를 아예 우회하는 방향으로 설계 변경. Chat 메시지를 간략한 정보(제목/작성자/문의ID/첨부 유무) + `상세 확인 및 완료 처리 → {링크}` 한 줄로 단순화하고, 링크가 여는 `inquiry-board/admin-detail.html`(+`js/admin-detail.js`)에서 전체 내용·이미지 원본·매칭 정보를 보여주고 완료 처리 버튼을 제공. `backend/src/config.js`에 `FRONTEND_BASE_URL` 추가(기본값 `http://localhost:8000`). 브라우저로 링크 클릭 → 상세 확인 → 완료 처리 → "완료 처리됨" 상태 전환, 이미지 첨부 건은 원본 이미지 렌더링까지 확인 완료.
  - 상태는 이번에도 완료/미완료 이진법 그대로 유지(사용자 확인), "진행중" 등 중간 상태 확장은 필요시 후속 과제로 남김
- [x] 6-6. (추가) **상세 페이지 링크를 순차 ID 대신 랜덤 accessToken으로 전환** — 로그인 붙기 전까지, 다른 팀 문의 번호(`Q-0002` 등)를 순회해 열람하는 걸 막기 위한 임시 보완책. 문의 생성 시 `crypto.randomUUID()`로 `accessToken` 발급, `GET /api/inquiries/by-token/:token`(신규 라우트, `/:id`보다 먼저 선언)으로만 상세 조회 가능하게 하고, Chat 링크(`admin-detail.html?token=...`)도 이 토큰을 쓰도록 변경. curl로 순차 ID를 토큰 자리에 넣으면 404, 실제 토큰이면 200 확인. 브라우저 렌더링은 이 세션의 정적 파일 서버 디스크 캐시 문제로 화면 확인은 못 했지만, API 응답과 프론트 모듈 함수(`getInquiryByToken`) 자체는 캐시를 우회해 직접 호출 검증 완료.
  - ⚠️ 여전히 남는 한계: 토큰이 유출되면(캡처/재공유 등) 만료 없이 계속 유효하고, 완료 처리자 신원도 로그인 전까지는 추적 불가 (기존 "관리자 인증 없음" 이슈와 동일 선상, SSO 연동(13-1) 전까지의 절충안)
- [x] 6-7. (추가) **Chat 메시지에 문의 내용 미리보기 재도입** — 6-5에서 링크만 남기며 빠졌던 문의 내용을 사용자 요청으로 다시 추가, 이번에는 50자로 제한하고 초과 시 "..."로 자른다 (`messageBuilder.js`의 `buildContentPreview`, 줄바꿈은 공백으로 합쳐 한 줄로 표시). `node -e`로 50자 이하/초과 케이스 둘 다 확인.

## Phase 7. 백엔드 — 완료 트리거 및 무응답 처리 ✅ (⚠️ 타임아웃 값은 PoC 테스트용 5초, 운영값 아님)

- [x] 7-1. `/api/chat/events` — 리액션(✅)/슬래시 명령 수신 → `completed_at`, `completed_by` 기록. 실제 Google Chat App이 없어 `{ inquiryId, actorEmail }` 형태의 단순화된 Mock 페이로드로 대체 (`backend/src/routes/chatEvents.js`). 매칭 실패 건 완료 시도 시 409 반환 확인, 이미 완료된 건은 멱등 처리 확인.
- [x] 7-2. 무응답 타임아웃 스케줄러 (`backend/src/scheduler/timeoutScheduler.js`) — ⚠️ **`backend/src/config.js`에 REMINDER_TIMEOUT_MS/ESCALATION_TIMEOUT_MS/SCHEDULER_INTERVAL_MS를 임시로 5초/5초/1초로 설정해 빠르게 검증함. 실제 운영값(근무시간 기준 2시간 / 2시간 / 15분 주기)이 아니므로 운영 배포 전 반드시 교체 필요** (config.js 상단 주석에도 명시해둠). curl로 리마인드 발송(`reminderSentAt`) → 에스컬레이션(`escalatedAt`) 순서로 정상 전이 확인, 완료된 건은 타임아웃 대상에서 제외되는 것도 확인.
  - ✅ (업데이트) Slack 웹훅으로 실제 테스트하면서 5초 설정이 "받자마자 멘션"으로 보여 혼동을 줘서, `backend/.env`에 `REMINDER_TIMEOUT_MS=7200000`, `ESCALATION_TIMEOUT_MS=7200000`(둘 다 실제 2시간)을 덮어써 적용. `config.js`의 5초 기본값 자체는 그대로 두고(추가 빠른 테스트 필요시 재사용), `.env`가 있으면 그 값이 우선 적용됨. curl로 6초 경과 후에도 `reminderSentAt`이 `null`로 남아있는 것 확인.

> 참고: 테스트 중 문의 등록과 완료 처리 curl 호출 사이의 실제 소요 시간이 5초에 근접하면서, 완료 처리 직전에 리마인드가 먼저 발송되는 경계 케이스를 관찰했습니다. 로직 버그가 아니라 "타임아웃 임박 시점에 완료 처리가 들어오면 리마인드와 경합할 수 있다"는 실제 운영에서도 나타날 수 있는 자연스러운 동작입니다.

## Phase 8. 백엔드 — 매칭 실패 운영 및 설문 발송 확정 ✅ (⚠️ SLA 값도 PoC 테스트용 5초, 운영값 아님)

- [x] 8-1. `GET /api/admin/unassigned` — 매칭 실패 후 미배정 목록, `slaExceeded` 플래그 포함 (`backend/src/config.js`의 `ADMIN_ASSIGN_SLA_MS`도 Phase 7과 동일하게 **임시 5초**, 운영값은 1영업일 — curl로 5초 전/후 `slaExceeded: false → true` 전환 확인)
- [x] 8-2. `POST /api/admin/inquiries/:id/assign-team` — 관리자 수동 팀 배정 → `sendInquiryToTeam` 공용 로직 재사용해 Chat 전송까지 재진입 확인, 배정 후 `unassigned` 목록에서 제외되는 것 확인
- [x] 8-3. `GET /api/admin/survey-pending` — 완료됐지만 설문 미발송 목록
- [x] 8-4. `POST /api/inquiries/:id/survey-ready` — 관리자 설문 발송 확정 (완료 전 문의에 대해서는 409로 거부)
- [x] 8-5. `GET /api/inquiries/pending-survey?contact={email}`, `POST /api/inquiries/:id/survey`, `GET /api/inquiries/:id` — 확정 전 null → 확정 후 노출 → 설문 제출 후 다시 null로 돌아오는 전체 사이클 curl로 확인

## Phase 9. 백엔드 — 문의자 알림 (이메일 발송, Mock) [신규 삽입] ✅ (⚠️ 9-2, 9-3 방식이 이후 변경됨 — 아래 참고)

- [x] 9-1. `MockEmailClient` 구현 (`backend/src/email/mockEmailClient.js`) — 실제 Gmail API 호출 없이 인메모리 로그 + 콘솔 출력, `sendEmail(to, subject, body)` 인터페이스로 실 연동 시 `LiveEmailClient`로 교체 가능하게 구성. 도메인 문구 조립은 `notifyInquirer.js`로 분리
- [x] ~~9-2. 매칭 성공 시 이메일 발송~~ → **제거**: 문의 결과가 웹페이지 응답으로 바로 표시되므로 제출 시점 이메일은 불필요하다는 피드백 반영
- [x] ~~9-3. 매칭 실패 시 이메일 발송~~ → **제거**: 같은 이유로 실패 직후에도 이메일 발송 안 함
- [x] 9-2'. (수정) **관리자가 매칭 실패 건을 수동 배정 완료했을 때만** 이메일 발송 — `routes/admin.js`의 `assign-team`에서 `notifyTeamAssigned` 호출. curl로 자동 성공/실패 직후엔 로그 비어있고, 수동 배정 후에만 1건 기록되는 것 확인
- [x] 9-4. `GET /api/mock-email/log` — 발송 로그 확인용 디버그 엔드포인트 (Phase 6의 `mock-chat/log`와 동일 패턴)

> ⚠️ 실제 Gmail API 연동은 Google Chat App(Phase 6)과 마찬가지로 사내 Workspace 관리자의 OAuth/서비스 계정 승인이 필요해 이번 PoC 환경에서는 Mock으로 대체한다. 실 연동은 Phase 13(후속 확장 과제, 13-4)로 이동.

## Phase 10. 프론트-백엔드 연동 (기존 Phase 9에서 번호 이동) ✅

- [x] 10-1. `inquiry-board/js/api.js`의 각 mock 함수를 실제 엔드포인트 fetch 호출로 교체 — `mock-data.js`(localStorage 기반 mock)는 더 이상 쓰이지 않아 삭제. `assignTeam`은 팀 이름 대신 `teamId`를 쓰도록 계약이 바뀌어 `admin-demo.js`도 함께 수정 (팀 목록은 신규 `GET /api/teams`로 조회). CORS 허용을 위해 백엔드에 `cors` 미들웨어 추가.
- [x] 10-2. 전체 흐름 통합 테스트 — 브라우저(프론트, `python -m http.server`) + 백엔드(`node src/index.js`) 동시 기동 후 실제 fetch로: 문의 등록(회계팀 매칭 63%) → 완료 처리(`/api/chat/events`) → 관리자 데모에서 설문 발송 확정 → 재접속 시 팝업 노출 → 설문 제출까지 DB(인메모리) 값으로 확인. 매칭 실패 건도 관리자 데모의 실제 팀 드롭다운(`GET /api/teams`)으로 수동 배정해 `unassigned`에서 빠지는 것까지 확인.

> 🐛 **연동 중 발견해 수정한 버그**: `inquiry.js`가 여전히 구버전 매칭 실패 사유(`no_team_found`)를 기준으로 문구를 분기하고 있었는데, Phase 5에서 백엔드가 2단계 파이프라인으로 바뀌며 실패 사유가 `no_candidate`로 변경된 걸 프론트에 반영하지 않은 상태였습니다. 그 결과 "가장 가까운 팀(추정: null)은 찾았지만..." 같은 깨진 문구가 떴던 것을 `no_candidate` 기준으로 수정했습니다.

## Phase 11. 문의 이미지 첨부 지원 [신규 삽입] ✅

오류 메시지 스크린샷 등 이미지로 문의하는 경우를 위한 첨부 기능. **이미지는 팀 전달용으로만 사용하고, 매칭 로직(키워드/LLM 재판단)에는 관여시키지 않는다** — 이미지 내용 자동 인식(OCR/비전)은 복잡도가 높아 후속 과제(13절)로 분리.

- [x] 11-1. 프론트: 문의 작성 화면에 이미지 업로드 필드(선택, 1장) 추가 (`index.html`)
- [x] 11-2. 백엔드: `POST /api/inquiries`가 `multipart/form-data`를 받아 이미지를 저장(`backend/uploads/`)하고 `imageUrl`을 문의 데이터에 기록 — `multer` 사용 (`backend/src/upload/uploadMiddleware.js`), 이미지 없이 JSON으로 보내는 기존 요청도 그대로 통과하도록 구성. 매칭 파이프라인 입력(제목+내용 텍스트)에는 영향 없음을 curl로 확인
- [x] 11-3. Mock Chat 전송 메시지에 첨부 이미지 유무/링크 표시 (`mockChatClient.js`) — curl로 "첨부 이미지: /uploads/..." 줄 포함 확인
- [x] 11-4. 민감정보 안내 문구에 "스크린샷도 팀 전체에 노출됩니다" 문구 추가
- [x] 11-5. 문의 상세 조회 시 이미지 URL 함께 반환, 프론트에서 링크로 노출 (`resolveUploadUrl`) — `/uploads` 정적 서빙 추가, 브라우저에서 이미지 없이 제출하는 기존 흐름도 회귀 없이 정상 동작 확인

> 참고: curl 테스트 중 한글 필드 값을 커맨드라인 인자로 바로 넘기면 이 환경 인코딩 때문에 깨지는 현상(Phase 5 때와 동일)을 다시 겪어서, `content` 필드는 UTF-8 파일에서 읽어 `-F "content=<file"`로 전달해 우회 검증했습니다. 서버 로직 자체의 문제는 아닙니다.

## Phase 12. 팀 관리자 페이지 (조직도 기반 키워드/설명/웹훅 관리) [신규 삽입] ✅

팀 담당자가 직접 자신의 웹훅 URL·키워드·설명을 등록할 수 있는 화면. 조직도는 "회사 DB에서 조회 가능"하지만 지금은 계층형 JSON으로 우선 구현하고, 실제 DB 연동은 후속 과제로 분리(14-7).

- [x] 12-1. 데이터 구조 분리: `backend/data/org-chart.json`(git 추적, 계층형 조직도 스켈레톤 — 리프 노드만 `isTeam: true`) / `backend/data/team-config.json`(git 미추적, 팀별 keywords/description/webhookUrl, 템플릿은 `team-config.example.json`)
- [x] 12-2. `backend/src/teams/teamRepository.js` 신설 — org-chart를 평탄화해 리프 팀 목록을 뽑고 team-config를 병합해 제공하는 단일 창구. `routes/inquiries.js`, `routes/admin.js`, `routes/teams.js`, `scheduler/timeoutScheduler.js`, `chat/sendInquiryToTeam.js`, `chat/chatClientRegistry.js`, `chat/liveChatClient.js`, `scripts/test-matching.js`에서 기존 `teams-seed.json` 직접 참조를 전부 교체. `teams-seed.json`과 이제 쓰이지 않는 `chat/webhookConfig.js`는 삭제. `node scripts/test-matching.js`로 매칭 결과가 리팩터링 전과 동일함을 확인
- [x] 12-3. `GET /api/org-chart` — 트리 구조 + 리프별 `hasConfig` 플래그 반환 (curl로 확인)
- [x] 12-4. `GET /api/teams/:id/config` — keywords/description + 마스킹된 웹훅 상태(`hasWebhook`, 끝 4자리) (curl로 확인)
- [x] 12-5. `PUT /api/teams/:id/config` — keywords/description/webhookUrl 저장. `webhookUrl`을 안 보내면 기존 값 유지되는 것 curl로 확인
- [x] 12-6. `POST /api/teams/:id/test-webhook` — 저장된 웹훅으로 테스트 메시지 즉시 전송, 실제 Slack 채널로 전송 성공 확인
- [x] 12-7. 프론트 `admin-teams.html`/`admin-teams.js` — 좌측 조직도 트리(리프만 클릭 가능, 🟢/⚪로 설정 여부 표시) + 우측 편집 폼(키워드 칩 입력, 설명 textarea, 마스킹된 웹훅 입력 + 변경 버튼, 테스트 전송 버튼, 저장 버튼). 브라우저로 트리 렌더링·팀 선택·폼 로딩까지 확인, 칩 추가 로직은 자바스크립트로 keydown 이벤트를 직접 발생시켜 검증(이 테스트 환경의 브라우저 자동화 도구가 실제 Enter 키 입력을 페이지에 제대로 전달하지 못하는 한계가 있어 우회 — 실제 사용자 키보드 입력에는 문제없음)
- [x] 12-8. 최소 접근 제어 — 공유 비밀번호(`ADMIN_PASSWORD`, 소스에 평문 노출) + `sessionStorage`. 브라우저 새로고침 시 재인증 없이 유지되는 것 확인. **소스만 봐도 비밀번호가 드러나는 수준이라 실제 보안장치가 아님을 화면에도 명시**
- [x] 12-9. 마이그레이션 — 기존 5개 팀(`teams-seed.json`) 키워드/설명/(로컬의 실제 Slack 웹훅 URL)을 `team-config.json`으로 이관, 임시 2단계 조직도(재무본부>회계팀 / 경영지원본부>인사팀·총무팀 / IT본부>IT지원팀 / 법무실>법무팀)에 배치. `backend/.env`의 `CHAT_WEBHOOK_*` 방식은 제거하고 `.env.example`/`.env`에서 관련 설명 정리

> ⚠️ Mock으로 남아있거나 후속 구현이 필요한 부분(추후 기록):
> - **조직도 데이터**: 여전히 `org-chart.json` 정적 스켈레톤 — "회사 DB에서 실시간 조회"는 미구현 (14-7)
> - **접근 제어**: 공유 비밀번호 하드코딩 — SSO 연동 전까지의 완전 임시 조치 (14-1)
> - **웹훅 URL 유효성 검증 없음**: 저장 시 형식 체크를 안 해서, 잘못된 URL을 넣어도 저장은 되고 "테스트 전송" 눌러야 실패를 알 수 있음
> - **description 필드는 여전히 죽은 데이터**: 관리자가 정성껏 입력해도 `MockLlmReranker`가 안 읽음 — 실 LLM 연동(14-2) 전까지는 입력해도 체감 효과 없음(화면에는 안내 문구로 명시해둠)

> ⚠️ `backend/.env`의 `CHAT_WEBHOOK_*` 방식(6-4)은 이 Phase 완료 후 `team-config.json` 기반으로 대체된다 — 팀이 늘어날 때마다 `.env`에 변수를 추가할 필요가 없어짐. 실제 조직도 DB 연동(회사 DB 실시간 조회)은 여전히 Mock/JSON 상태이며 후속 과제(14-7)로 기록.

## Phase 13. 실제 LLM API 연동 (`LiveLlmReranker`) [신규 삽입] ✅

Mock으로 흉내만 내던 2단계 재판단을 실제 Anthropic Claude API로 교체. 팀 관리자 페이지(Phase 12)에서 채워진 `description`이 이제 처음으로 실제 프롬프트에 쓰인다.

- [x] 13-1. `@anthropic-ai/sdk` 설치, `backend/src/matching/liveLlmReranker.js` 구현 — 후보 팀들의 `description`(teamRepository)+문의 원문을 프롬프트로 구성, tool use로 `{teamId, confidence, reason}` 구조화 출력 강제, `temperature: 0`
- [x] 13-2. 모델은 `claude-haiku-4-5-20251001` 채택 — 근거: 후보 3~5개 중 하나를 고르는 얕은 분류 작업(후보 필터링이 입력 크기를 항상 3~5개로 고정하므로 팀 수·설명 길이가 늘어도 입력 규모는 안 늘어남), 문의 등록이 동기 호출이라 응답 속도가 중요, 호출 빈도가 잦아 비용 민감, `low_confidence` 실패 시 사람이 검토하는 안전망이 이미 있어 완벽한 판단력이 필요 없음 — 실제 정확도 데이터(4절)로 부족함이 확인되기 전까지는 상위 모델로 올릴 근거 없음
- [x] 13-3. `backend/src/matching/rerankerRegistry.js` — `ANTHROPIC_API_KEY`가 `.env`에 있으면 Live, 없으면 Mock (팀별 웹훅 자동 분기와 동일 패턴)
- [x] 13-4. API 호출 실패(네트워크 오류/잘못된 모델명 등) 시 `MockLlmReranker`로 자동 폴백 — 실제로 모델명 오류(404)로 폴백되는 것과, 이후 올바른 모델로 정상 응답되는 것 둘 다 확인
- [x] 13-5. **민감정보 마스킹 신규 구현** — `backend/src/security/maskSensitiveInfo.js` (주민번호/카드번호/전화번호/이메일 정규식 마스킹), `liveLlmReranker.js`가 프롬프트 구성 전에만 적용 (Chat 메시지·상세 페이지는 팀원이 실제 처리해야 해서 마스킹 안 함). 4개 패턴 모두 마스킹되고 일반 텍스트는 그대로 통과하는 것 확인
- [x] 13-6. `matching/index.js`를 `async`로 전환(`await reranker.rerank(...)`), `matcherMode`를 재판단기 응답에서 직접 받도록 변경(폴백 시 실제로 `llm_mock`이 찍히도록). `routes/inquiries.js`, `scripts/test-matching.js` 호출부 `await` 반영
- [x] 13-7. `node scripts/test-matching.js`로 회귀 확인(Mock 모드) + 실제 API로 재검증: 명확한 케이스는 confidence 0.95, 애매한 케이스("회의실+계약서")는 confidence 0.6과 함께 "왜 총무팀을 골랐고 법무팀 연계가 필요할 수 있는지"까지 근거를 남김 — Mock의 산술식으로는 못 하던 부분
- [x] 13-8. 🐛 (수정) `matchReason`이 백엔드엔 저장되는데 `admin-detail.html`에 표시가 안 되던 것을 발견 — "매칭 판단 근거" 항목으로 상세 페이지에 추가, 브라우저에서 실제 노출 확인

> ⚠️ Mock/추후 구현 기록:
> - Mock 폴백은 그대로 유지 (14-신규 항목 아님, 이미 완료) — 다만 **API 실패가 잦아지면 관리자가 알 방법이 없음**(로그에만 남음). 장애 감지/알림은 후속 과제
> - 모델 등급은 지금 판단 기준(작업 단순성/속도/비용/안전망)으로 정했고, 실측 정확도가 쌓이면(4절) 재검토 필요

## Phase 14. 관리자 데모 제거 + 문의자용 "내 문의 목록/상세" [신규 삽입] ✅

문의게시판 하단의 "관리자 데모" 섹션(Phase 3, 8-1/8-2/8-4 프론트)을 완전히 제거하고, 그 안에서 하던 수동 처리(매칭 실패 배정, 설문 발송 확정)는 UI 없이 백엔드 API만 남긴다. 대신 **문의자 본인이 자신의 문의 상태를 조회**하는 화면을 추가한다.

- [x] 14-1. `inquiry-board/index.html`에서 관리자 데모 섹션 마크업 + 관련 스크립트 제거, `js/admin-demo.js` 삭제. `js/inquiry.js`의 `window.refreshAdminDemo()` 호출부도 제거
- [x] 14-2. `js/api.js`에서 관리자 데모 전용이었던 `markSurveyReady`/`listSurveyPending`/`listUnassigned`/`assignTeam`/`getTeams` 제거 (다른 곳에서 안 쓰는 것 확인 후 삭제) — 백엔드의 해당 API(`/api/admin/*`)는 그대로 유지, 프론트 UI만 없앤 것
- [x] 14-3. `GET /api/inquiries?contact={email}` 신규 — 본인이 제출한 문의 전체를 최신순으로 조회 (`routes/inquiries.js`)
- [x] 14-4. **본인 확인은 로그인 붙기 전까지 고정된 데모 사용자 한 명(`demo-user@example.com`)으로 대체** — 사용자 결정사항. 실제 로그인(SSO, 15-1) 연동 시 이 상수를 로그인 세션 이메일로 교체하면 됨
- [x] 14-5. `inquiry-board/my-inquiries.html`/`js/my-inquiries.js` — 목록 화면, 각 항목 클릭 시 상세로 이동
- [x] 14-6. `inquiry-board/my-inquiry-detail.html`/`js/my-inquiry-detail.js` — **읽기 전용** 상세 화면. 매칭팀/신뢰도/AI 판단 근거/팀 전달 상태/처리 상태/설문 상태까지 전부 노출(사용자 결정사항), 완료 처리 등 액션 버튼은 없음(그건 팀이 Chat 링크로 하는 일)
- [x] 14-7. `js/inquiryStatus.js` 신설 — 목록의 한 줄 요약과 상세의 상태 설명을 공유하는 로직으로 분리
- [x] 14-8. 브라우저로 전체 확인: 관리자 데모 섹션 완전히 사라진 것, 매칭 성공 건("처리중 (담당팀: 회계팀)")과 실패 건("담당자 배정 대기중") 둘 다 목록/상세에 정확히 반영되는 것, 상세에 AI 판단 근거까지 노출되는 것 확인

> ⚠️ Mock/추후 구현 기록:
> - **본인 확인이 이메일 일치만으로 이뤄짐** — 로그인 전까지는 `demo-user@example.com`으로 제출한 사람이면 실제로 누구든 그 이메일만 알면 목록을 볼 수 있음(다른 이메일 목록 조회 API 자체는 없지만, 안다면 그 이메일로 조회 가능). SSO 연동(15-1) 전까지의 임시 상태
> - 관리자용 수동 처리(매칭 실패 배정, 설문 발송 확정) UI가 없어짐 — 지금은 `curl`이나 향후 정식 관리자 페이지로만 가능. 정식 관리자 페이지는 여전히 미착수

## Phase 15. 담당자 배정 + 전체 생명주기 상태 로그 [신규 삽입] ✅

담당팀 배정 → 완료 처리 사이에 "담당자 배정" 단계를 추가. 완료 처리와 순서를 강제하지 않으며(사용자 결정사항), 문의 하나의 전체 생명주기(접수/매칭/배정/담당자지정/완료/설문 등)를 타임스탬프 로그로 남긴다.

- [x] 15-1. `backend/data/team-members.json` 신설 — 팀별 담당자 후보 명단을 하드코딩(실제로는 회사 인사 DB에서 조회할 데이터의 자리, `org-chart.json`과 동일한 패턴). `teamRepository.getTeamMembers(id)` 추가
- [x] 15-2. `store/inMemoryStore.js`에 `appendStatusLog(id, event, detail)` 추가 — 문의 객체에 `statusLog: [{at, event, detail}]` 배열을 누적. 문의 생성 시 `created`/`matched`(또는 `match_failed`) 최초 로그 기록
- [x] 15-3. `POST /api/inquiries/:id/assignee` 신규 — `teamRepository.getTeamMembers`로 후보 검증 후 `assigneeId/assigneeName/assigneeAssignedAt` 갱신 + `assignee_assigned` 로그. 팀이 배정되지 않은 문의는 409로 거부
- [x] 15-4. `GET /api/teams/:id/members` 신규 — 담당자 배정 드롭다운용 후보 목록
- [x] 15-5. 기존 라우트들에도 상태 로그 추가: Chat 전송 실패(`chat_send_failed`, `routes/inquiries.js`/`routes/admin.js`), 관리자 수동 팀 배정(`team_assigned_manual`, `routes/admin.js`), 완료 처리(`completed`, `routes/chatEvents.js`), 설문 발송 확정(`survey_ready`)/응답 완료(`survey_answered`, `routes/inquiries.js`)
- [x] 15-6. 프론트 `js/api.js`에 `getTeamMembers`/`assignInquiryOwner` 추가. `js/inquiryStatus.js`에 담당자 상태 줄 추가 + `renderStatusLogHtml()` 신설(상세 화면 공용 로그 렌더링)
- [x] 15-7. `admin-detail.html`(Chat 링크로 여는 페이지) — 담당자 드롭다운 + "담당자 지정" 버튼(완료 처리와 순서 무관), 처리 이력(전체 로그) 표시 추가
- [x] 15-8. `my-inquiry-detail.js`(문의자 읽기 전용 화면)에도 처리 이력 로그 표시 추가 (Phase 14의 "다 보여준다" 결정과 일관되게 담당자/로그도 노출)
- [x] 15-9. `css/style.css`에 `.assignee-box`/`.status-log`/`.status-log-time` 스타일 추가
- [x] 15-10. `curl`로 `POST /assignee`, `GET /teams/:id/members` 동작 확인 및 `statusLog` 누적 확인 (매칭 성공 → 담당자 지정까지 로그 2건→3건으로 쌓이는 것 확인)

> ⚠️ Mock/추후 구현 기록:
> - **팀원 명단(`team-members.json`)이 하드코딩** — 실제로는 회사 인사 DB에서 조회해야 할 데이터. 사용자 결정사항으로 지금은 관리 UI도 만들지 않음(admin-teams.html에 추가하지 않기로 최종 결정). 후속 과제로 실 DB 연동 시 `teamRepository.getTeamMembers()` 내부 구현만 교체하면 됨
> - 담당자 지정에 순서 강제가 없어(사용자 결정사항), 담당자 없이 완료 처리된 건이 존재할 수 있음 — 의도된 동작

## Phase 16. 문의 취소 기능 [신규 삽입] ✅

담당팀 배정 → 완료 처리 사이에 "취소" 액션을 추가. 완료와 취소는 상호 배타적인 최종 상태로 취급한다(사용자 결정사항 — 완료 전에만 취소 가능, 매칭 실패 건은 아직 담당자가 없어 취소 대상에서 제외).

- [x] 16-1. `routes/inquiries.js`에 `cancelledAt/cancelledBy/cancelReason/cancelDetail` 필드 추가, `POST /:id/cancel` 신규 — 취소 사유 카테고리(`중복 문의`/`문의자 철회 요청`/`처리 불가`/`기타`, 사용자 결정사항으로 기본 4종) 검증 + 상세 텍스트 저장 + `cancelled` 상태 로그 기록. 팀 미배정 건은 409, 이미 완료된 건은 409, 이미 취소된 건은 멱등하게 그대로 반환
- [x] 16-2. 상태 간 상호배제 가드 추가 — `routes/chatEvents.js`(완료 처리)와 `routes/inquiries.js`(담당자 지정) 둘 다 이미 취소된 문의에 대해 409로 거부하도록 추가
- [x] 16-3. 프론트 `js/api.js`에 `cancelInquiry`/`CANCEL_REASON_OPTIONS` 추가. `js/inquiryStatus.js`의 `summaryLabel`/`statusDetails`에 취소 상태 분기 추가(취소된 건은 완료/설문 진행 안내를 더 보여주지 않음)
- [x] 16-4. `admin-detail.html`(관리자/담당자 상세 페이지) — "취소" 버튼 클릭 시 사유 카테고리 select + 상세 텍스트 textarea가 있는 폼이 펼쳐지고, 제출하면 취소 처리. 취소/완료된 건은 액션 버튼(완료 처리/취소/담당자 지정) 없이 상태 안내만 표시
- [x] 16-5. `my-inquiry-detail.js`(문의자 상세 페이지)에도 취소 상태가 자동으로 노출됨(공용 `statusDetails`/`renderStatusLogHtml` 재사용이라 별도 구현 불필요)
- [x] 16-6. `css/style.css`에 `.cancel-box` 스타일 추가
- [x] 16-7. curl로 상태 전이 전부 확인: 매칭 실패 건 취소 시도(409), 잘못된 카테고리(400), 정상 취소, 취소된 건 완료/담당자 지정 시도(둘 다 409), 취소 재요청(멱등 200), 완료된 건 취소 시도(409). 브라우저에서 취소 폼 제출 → 액션 버튼 사라짐 → 문의자 상세 페이지에도 취소 사유 노출까지 확인, `<script>` 페이로드를 상세 텍스트에 넣어 이스케이프되는 것도 재확인

## Phase 17. 파일럿 실행 (기존 Phase 15에서 번호 이동)

- [ ] 17-1. 1~2개 팀 대상 소프트 런칭
- [ ] 17-2. 매칭 실패율, 무응답 SLA 경과 건수, 팝업 노출/응답률 모니터링
- [ ] 17-3. 50~100건 데이터 축적 후 1차 매칭 정확도 산출 (4절)

## Phase 18. (후속, 별도 승인 필요) 확장 과제 (기존 Phase 16에서 번호 이동)

- [ ] 18-1. 사내 구글 SSO 로그인 연동 → `contact`(이메일) 기반 식별을 로그인 세션으로 교체, Phase 12-8의 임시 비밀번호 게이트와 Phase 14-4의 고정 데모 사용자 둘 다 대체
- [ ] 18-2. 과거 정답 사례 기반 임베딩/RAG 하이브리드 전환 (description 대신/추가로 few-shot 예시 활용)
- [ ] 18-3. 실제 Gmail API 연동 (`LiveEmailClient`) — 사내 Workspace 관리자 승인 후
- [ ] 18-4. 매칭 실패 시 관리자 즉시 알림(push) 및 `low_confidence` 문의자 셀프 선택 — 대기시간 보완 방안 (논의만 완료, 구현 보류)
- [ ] 18-5. 첨부 이미지 내용 자동 인식(OCR/비전 LLM) → 매칭 파이프라인 입력에 포함 (Phase 11에서 의도적으로 제외한 부분)
- [ ] 18-6. 팀원 명단(`team-members.json`)을 실제 회사 인사 DB 조회로 교체 + (필요 시) admin-teams.html에 명단 관리 UI 추가
- [ ] 18-7. 조직도를 JSON 스켈레톤(Phase 12-1) 대신 회사 DB에서 실시간 조회하도록 교체
- [ ] 18-8. LLM API 장애 감지/알림 (13절에서 발견된 갭)
- [ ] 18-9. 정식 관리자 페이지 (매칭 실패 수동 배정 + 설문 발송 확정 UI) — Phase 14에서 데모 UI 제거 후 아직 대체품 없음

---

## 부록. Mock/임시 처리/미구현 항목 총정리

지금까지 구현한 것 중 **실제 서비스 배포 전 반드시 다시 봐야 할 항목**을 한곳에 모았다. 각 항목은 해당 Phase에도 개별적으로 표시돼 있으니, 여기서는 "무엇이 진짜가 아닌지"만 빠르게 훑어볼 수 있게 정리한다.

### A. 외부 연동을 Mock으로 대체한 것 (실제 서비스 붙이려면 계정/승인 필요)

| 항목 | 현재 상태 | 실제로 필요한 것 | 관련 Phase |
|---|---|---|---|
| LLM 재판단 | (Phase 13에서 실 연동 완료) `ANTHROPIC_API_KEY`가 있으면 `LiveLlmReranker`(Claude Haiku 4.5)가 실제로 호출됨, 없거나 실패하면 `MockLlmReranker`로 자동 폴백 | 사내 보안팀의 정식 검토·승인 (지금은 개인 판단으로 연동한 상태) | 5-3, 13 |
| Chat 전송 | 팀별 웹훅(Phase 12부터 `backend/data/team-config.json`에서 관리, 관리자 페이지에서 등록)이 설정되면 `LiveChatClient`로 실제 전송, 없으면 `MockChatClient`(콘솔+인메모리 로그) — 현재 5개 팀 전부 **Slack** 웹훅(같은 URL)에 연결해 테스트 중, Google Chat 웹훅으로 바꿔도 계약이 동일해 코드 변경 없이 그대로 동작 | Google Chat App 정식 등록 + 팀별 실제 스페이스 웹훅 발급 (지금은 Slack 임시 대체) | 6-4, 12 |
| 완료 트리거(리액션 웹훅) | `POST /api/chat/events`가 실제 Google 이벤트 대신 단순화된 `{ inquiryId, actorEmail }` 페이로드를 받음 (`backend/src/routes/chatEvents.js`) | Workspace Events API + Cloud Pub/Sub 구독, 이벤트 서명 검증 | 7-1 |
| 이메일 발송 | `MockEmailClient` — 실제 Gmail API 대신 콘솔 출력 + 인메모리 로그 (`backend/src/email/mockEmailClient.js`) | Gmail API OAuth/서비스 계정 승인 | 9-1 / 후속 13-4 |
| 사용자 로그인 | 로그인 없음 — 문의 작성 시 입력한 연락처(이메일)를 `localStorage`의 `myContact`로 저장해 임시 식별자로 사용 | 사내 구글 SSO 연동 | 후속 13-1 |
| 팀 배정 관리자 인증 | 관리자 액션(`assign-team`, `survey-ready`, 완료 트리거)에 실제 권한 검사 없음 — 아무나 호출 가능 | 관리자 인증/권한 체계 | 미착수 |

### B. 데이터 저장을 임시로 처리한 것

| 항목 | 현재 상태 | 실제로 필요한 것 | 관련 Phase |
|---|---|---|---|
| 문의/설문 데이터 저장소 | `backend/src/store/inMemoryStore.js` — 서버 프로세스 안 배열에만 저장, **재시작하면 전부 사라짐** | PostgreSQL 연결 + repository 계층으로 교체 | 5-4 / 미착수(구 Phase 9 DB 연동 항목) |
| PostgreSQL 스키마 | 마이그레이션(`backend/migrations/001~004`)과 시드(`backend/seeds/001_teams.sql`)는 작성했지만, 이 환경에 PostgreSQL이 없어 **실행 검증을 못 함** | 실제 DB에 `npm run migrate`/`npm run seed` 실행해 문제 없는지 확인 | 4-1, 4-2 |
| 팀 R&R 데이터 중복 | `backend/seeds/001_teams.sql`(DB용)과 `backend/src/matching/teams-seed.json`(매칭 로직이 실제로 읽는 파일)이 **서로 다른 파일에 같은 내용을 중복 보관** 중 | DB 연동 후에는 매칭 로직이 `teams-seed.json` 대신 실제 `teams` 테이블을 조회하도록 교체 | 5-4 |
| Chat 스페이스 ID | (Phase 12에서 해소) 웹훅 URL 자체가 목적지를 담고 있어 별도 스페이스 ID 개념 자체를 제거함 (`teams-seed.json`과 함께 삭제) | Google Chat App 정식 연동 시 필요하면 재검토 | 6-2 → 12 |
| 첨부 이미지 저장소 | `backend/uploads/` 로컬 디스크에 저장 — 서버 재배포/다중 인스턴스 환경에서는 파일 유실 위험, 용량 제한/자동 정리 로직도 없음 | 클라우드 오브젝트 스토리지(S3류) 연동, 용량/보관기간 정책 수립 | 11-2 |
| 팀원(담당자) 명단 | `backend/data/team-members.json` — `org-chart.json`과 동일한 패턴으로 하드코딩. 사용자 결정사항으로 관리 UI도 만들지 않음 | 회사 인사 DB에서 팀별 소속 인원을 실시간 조회 | 15-1 |

### C. 운영값을 테스트용으로 낮춰놓은 것 (⚠️ 배포 전 반드시 교체)

모두 `backend/src/config.js`에 있고, 파일 상단에도 동일 경고를 남겨뒀다.

| 설정값 | 현재(PoC 테스트) | 실제 운영 목표값 |
|---|---|---|
| `REMINDER_TIMEOUT_MS` (무응답 리마인드) | ✅ 2시간으로 교체 완료 (`backend/.env`) | 2시간 (근무시간 기준) |
| `ESCALATION_TIMEOUT_MS` (리마인드 후 에스컬레이션) | ✅ 2시간으로 교체 완료 (`backend/.env`) | 2시간 |
| `SCHEDULER_INTERVAL_MS` (타임아웃 체크 주기) | 1초 | 15분 |
| `ADMIN_ASSIGN_SLA_MS` (매칭 실패 수동 배정 SLA) | 5초 | 1영업일 |

### D. 정식 화면 대신 임시 데모로 대체한 것

| 항목 | 현재 상태 |
|---|---|
| 관리자 페이지 (문의 트리아지) | Phase 14에서 데모 UI(`admin-demo.js`)까지 완전히 제거됨 — 정식 페이지도 데모 페이지도 없음. 매칭 실패 수동 배정/설문 발송 확정은 이제 `curl`로 백엔드 API를 직접 호출하는 방법뿐 (16-8) |
| 관리자 페이지 (팀 관리) | Phase 12에서 `admin-teams.html`로 별도 구현됨 — 공유 비밀번호 게이트만 있고 실제 인증은 아님 (아래 보안 항목 참고) |
| 완료 트리거 UI | (6-5로 해소) Chat 메시지의 "상세 확인 및 완료 처리" 링크 → `admin-detail.html`에서 완료 처리 버튼 클릭. 인증 없이 링크만 있으면 누구나 완료 처리 가능한 점은 여전히 미해결(위 "팀 배정 관리자 인증" 항목과 동일 이슈) |

### E. 보안/설정상 임시로 느슨하게 둔 것

- 백엔드 CORS가 모든 origin을 허용 (`app.use(cors())`, 옵션 없음) — 운영 배포 시 프론트 도메인으로 제한 필요
- `POST /api/chat/events`의 `actorEmail`, `POST /api/inquiries/:id/survey-ready`의 `adminEmail` 등은 클라이언트가 보낸 값을 그대로 신뢰 — 실제로는 인증된 세션에서 가져와야 함
- 매칭 실패 시 문의 원문이 결국 LLM/사람에게 그대로 노출되는 구조라, 3.3절 민감정보 안내 문구 외의 자동 필터링은 여전히 없음 (PoC 범위 밖으로 원래부터 확정된 사항)
- 상세 페이지 `accessToken`(6-6)은 순차 ID 순회는 막지만, 링크 자체가 유출되면 만료/회수 없이 계속 유효함 — 로그인(SSO) 전까지의 절충안이지 완전한 인증은 아님
- 팀 관리자 페이지(`admin-teams.html`, 12-8)의 비밀번호가 프론트 소스에 평문으로 박혀 있어, 소스만 보면 누구나 알 수 있음 — 최소한의 진입장벽일 뿐 실제 인증이 아님
- `PUT /api/teams/:id/config`가 `webhookUrl` 형식을 검증하지 않음 — 잘못된 URL을 저장해도 에러 없이 통과하고, "테스트 전송"을 눌러야만 실패를 알 수 있음
- `admin-teams.html`도 `admin-detail.html`과 마찬가지로 인증된 세션 없이 값을 그대로 신뢰 — 관리자 액션 전반의 공통 이슈(위 `actorEmail`/`adminEmail` 항목과 동일 선상)
- `GET /api/inquiries?contact={email}`(14-3, "내 문의 목록")은 이메일이 일치하기만 하면 인증 없이 그 사람 명의의 문의를 전부 보여줌 — 지금은 고정된 데모 사용자 한 명만 쓰지만, 실사용 시 남의 이메일을 알면 그 사람 문의를 볼 수 있다는 뜻. SSO 연동(16-1) 전까지 감수해야 하는 한계

### F. 아직 논의만 하고 구현을 보류한 것

- 매칭 실패 시 관리자에게 즉시 알림(push) — 지금은 관리자가 "관리자 데모" 화면을 직접 열어봐야 알 수 있음 (13-5)
- `low_confidence` 케이스에서 문의자가 후보 팀 중 직접 선택하는 셀프 서비스 옵션 (13-5)
- 개인 단위 정밀 매칭, 임베딩/RAG 하이브리드 전환 (13-3)

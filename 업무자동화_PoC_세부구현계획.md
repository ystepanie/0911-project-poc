# 업무자동화 PoC 세부 구현 계획
## 문의 라우팅 자동화 (조직도 매칭 → Google Chat 전달 → 만족도 조사)

---

## 1. 배경 및 목표

**해결하려는 문제**: 타 부서 협조가 필요할 때 담당자를 몰라서 여러 사람에게 물어보거나, 파편화된 R&R 문서를 직접 뒤져야 하는 비효율.

**논의를 거쳐 확정된 PoC 방향**: 개인 담당자 자동 배정(완전 자동화)은 초기 리스크가 크다고 판단, 아래와 같이 스코프를 축소하여 최소 기능으로 시작.

**최종 확정 플로우**
```
직원A 문의 작성 및 확인 클릭
   ↓
조직도 검색 기반 업무 매칭 (팀 단위)
   ↓
해당 팀 Google Chat으로 전송
   ↓
팀원이 수동으로 담당자 배정 및 처리
   ↓
문제 해결
   ↓
일정 주기 만족도 조사 팝업 → 데이터 취합
```

**설계 원칙**: 개인이 아닌 팀 단위 매칭으로 낮춰서 퇴사자·연차자 리스크, 오답 반송 로직, 초기 라벨링 부담을 우회하고, 최종 배정 판단은 사람(팀원)에게 맡긴다.

---

## 2. 구현 범위 (Must-have / Nice-to-have / Deferred)

| 우선순위 | 항목 | 비고 |
|---|---|---|
| 필수 | 문의 고유 ID 발급 및 전 구간 추적 | 데이터 분석의 전제조건 |
| 필수 | 조직도 기반 팀 매칭 (RAG) | 핵심 기능 |
| 필수 | Google Chat 연동 (팀별 전송) | 핵심 기능 |
| 필수 | 완료 트리거 (수동 액션 고정) | 설문 발송 기준점 |
| 필수 | 무응답 타임아웃 / 리마인드 | 방관자 효과 대응 |
| 필수 | 만족도 조사 + 매칭 정확도 문항 분리 | 성능 측정과 만족도 측정 분리 |
| 필수 | 매칭 실패 시 안내 메시지 | 팀 매칭 자체가 안 되는 경우 |
| 권장 | 민감정보 안내 문구 (작성 화면) | 최소 비용 리스크 완화 |
| 보류 | 기존 채팅방 vs 신규 스페이스 구조 | 파일럿 데이터 확인 후 결정 |
| 보류 | HR/캘린더 시스템 연동 (퇴사자·연차자) | 팀 단위 매칭으로 리스크 완화되어 당장 불요 |
| 보류 | 개인 단위 정밀 매칭, few-shot/RAG 사례 보강 | 데이터 축적 후 2단계 과제 |
| 보류 | 파인튜닝 | 데이터 수백~수천 건 확보 후 검토 |

---

## 3. 상세 구현 계획

### 3.1 문의 접수 및 ID 체계

- 문의 게시판에서 "확인" 클릭 시 고유 문의 ID(예: `Q-0001`) 발급.
- 이 ID는 이후 모든 구간(팀 챗 메시지, 완료 처리 로그, 만족도 설문)에 동일하게 태깅되어야 함.
- 저장 항목(최소):
  - 문의 ID, 작성자, 문의 원문, 작성 시각
  - 매칭된 팀, 매칭 신뢰도 점수(있다면)
  - Google Chat 전송 시각 / 메시지 링크
  - 완료 처리 시각, 완료 처리자
  - 매칭 실패 여부 및 사유 카테고리

### 3.2 조직도 기반 팀 매칭 (RAG)

- 대상: 개인이 아닌 **팀 단위** R&R 정보. 각 팀의 책임 업무를 러프하게라도 문서화(예: "회계팀 - 법인카드, 정산, 세금계산서 관련").
- 매칭 실패(신뢰도 임계값 미만) 시 사용자에게 "관련 팀을 찾지 못했습니다" 안내 + 관리자 페이지로 자동 이관.
- 매칭 실패 사유는 아래 두 카테고리로 반드시 분리 로깅:
  - **팀 자체를 못 찾음** (recall 문제 → R&R 문서 보강 필요 신호)
  - **팀은 찾았으나 신뢰도 낮음** (precision 문제 → 매칭 로직/문서 품질 개선 신호)

### 3.3 Google Chat 연동

- 팀별 스페이스(또는 기존 팀 채팅방)에 봇을 통해 문의 내용 + 문의 ID + 문의자 정보를 전송.
- 초기 파일럿(3.6절)에서는 **신규 전용 스페이스**로 시작 권장 — 기존 업무 채팅과 섞여 묻히는 리스크를 먼저 배제하고, 필요 시 기존 방 통합 여부를 데이터로 판단.
- 문의 내용에 민감정보(급여, 인사평가, 법적 이슈 등)가 포함될 수 있음을 감안해, **문의 작성 화면에 안내 문구**를 노출 (예: "급여/인사평가/법적 분쟁 관련 문의는 OO 채널을 이용해주세요"). 자동 필터링은 이번 범위에서 제외.

### 3.4 배정 및 완료 처리

- 팀원 중 누가 처리할지는 수동(채팅 내 논의)으로 결정 — 시스템 개입 없음.
- **완료 트리거를 명시적 액션으로 고정**: 담당 처리자가 메시지에 정해진 이모지 반응(예: ✅) 또는 슬래시 명령을 입력하면 "완료" 처리.
- **무응답 타임아웃**: 전송 후 N시간(예: 2시간, 근무시간 기준) 내 반응이 없으면 팀장 멘션으로 자동 리마인드. 그래도 응답 없으면 관리자 페이지에 노출.

### 3.5 만족도 조사 설계

완료 처리 트리거 발생 시점 기준으로 일정 주기(즉시 또는 배치)로 문의자에게 팝업 발송. 설문 문항은 **목적이 다른 두 가지를 분리**하여 구성:

| 구분 | 문항 예시 | 목적 |
|---|---|---|
| 전반 만족도 | 문의 처리 과정에 만족하셨나요? (1~5점) | 서비스 전반 경험 측정 |
| 매칭 정확도 | 처음 연결된 팀이 올바른 담당 팀이었나요? (예/아니오) | **매칭 알고리즘 자체의 성능 측정** — 나중에 팀 매칭 로직 개선의 핵심 데이터 |
| (선택) 자유 의견 | 개선 의견 | 정성적 피드백 |

- 이 두 지표를 분리해야, "처리는 늦었지만 결국 해결되어 만족도는 높은" 케이스와 "매칭 자체가 틀렸던" 케이스를 구분해서 분석 가능.

### 3.6 파일럿(소프트 런칭) 계획

- 전사 오픈 전 **1~2개 팀 대상**으로 먼저 운영.
- 확인할 것: 방관자 효과 발생 빈도, 민감정보 노출 사례, 매칭 실패율, Google Chat 노출 방식(전용 스페이스 vs 기존 방)의 반응.
- 파일럿 기간 목표: 문의 건수 50~100건 확보 (다음 단계 라벨셋과 연동 가능).

---

## 4. 데이터 축적 및 성능 측정 계획 (2단계 준비)

이번 PoC 구조 자체가 "운영하면서 자연스럽게 라벨 데이터를 쌓는" 방식이라는 점을 활용한다.

### 4.1 라벨(정답) 데이터 확보 방식

- 매 문의 건마다 아래가 자동으로 기록됨:
  - 시스템이 매칭한 팀
  - 문의자가 응답한 "매칭 정확도" 설문 결과 (예/아니오)
- 이 두 값을 결합하면 곧 골드 라벨셋이 됨: 매칭된 팀 = 예측, 설문 결과 = 정답 여부.
- 목표 표본: 50~100건 축적 시 1차 정확도 산출.

### 4.2 측정 시 유의할 편향(Bias)

- 팀원이 잘못 배정된 문의를 신고하지 않고 그냥 처리해버리면 오답이 "성공"으로 기록될 수 있음 → 정확도가 실제보다 낙관적으로 나올 가능성 인지.
- 필요 시 소규모 샘플에 대해 별도 수동 감사(audit) 절차를 두어 보정.

### 4.3 축적 데이터의 향후 활용

| 활용 방식 | 적용 시점 | 비고 |
|---|---|---|
| Few-shot 프롬프트 예시 | 데이터 소량(수십 건)부터 즉시 적용 가능 | 구현 부담 최소 |
| RAG 인덱스에 과거 사례 추가 | 데이터 축적과 동시 병행 | 이번 구조(RAG)와 가장 잘 맞음, R&R 문서 부실 부분 보완 |
| 파인튜닝 | 데이터 수백~수천 건 이상 확보 후 검토 | 조직 개편이 잦은 환경 특성상 유지보수 부담 큼 — 우선순위 낮음 |

---

## 5. 리스크 및 대응 요약

| 리스크 | 발생 지점 | 대응 |
|---|---|---|
| R&R 문서 파편화/미비 | 팀 매칭 정확도 | 파일럿 병행하며 문서 보강, 실패 로그로 우선순위 파악 |
| 방관자 효과 | Google Chat 전송 후 | 무응답 타임아웃 + 팀장 리마인드 |
| 민감정보 노출 | 팀 전체 공개 채팅 | 작성 화면 안내 문구 (자동 필터링은 2단계 과제) |
| 완료 시점 불명확 | 만족도 조사 발송 기준 | 명시적 완료 트리거(이모지/명령어) 고정 |
| 정확도 측정 편향 | 성능 데이터 신뢰도 | 편향 가능성 인지 + 필요 시 수동 감사 |
| 매칭 정확도와 만족도 혼동 | 지표 해석 | 설문 문항 분리 설계 |

---

## 6. 단계별 로드맵

**Phase 0 (설계 확정)**
- 팀별 R&R 러프 문서화, ID 체계, 완료 트리거 규칙, 설문 문항 확정

**Phase 1 (개발 및 파일럿)**
- 문의 접수 → 조직도 매칭 → Google Chat 전송 → 타임아웃 로직 → 만족도 설문 구현
- 1~2개 팀 대상 소프트 런칭, 50~100건 데이터 축적

**Phase 2 (측정 및 개선)**
- 매칭 정확도 산출, 실패 유형(recall/precision) 분석
- Few-shot 예시 또는 RAG 사례 보강 적용

**Phase 3 (확장 검토, 데이터 기반 의사결정)**
- 전사 확대 여부, 개인 단위 정밀 매칭 재검토, HR/캘린더 연동 필요성 판단, 민감정보 자동 필터링 검토

# 실제 값은 이 파일을 복사한 backend/.env 에 채워 넣으세요 (.env는 git에 커밋되지 않습니다).
# 값을 채운 팀만 Google Chat으로 실제 전송되고, 비워둔 팀은 계속 Mock으로 동작합니다 (chatClientRegistry.js).
# 스페이스 설정 > 앱 및 통합 > 웹훅 에서 발급받은 URL을 그대로 붙여넣으면 됩니다.

CHAT_WEBHOOK_ACCOUNTING=
CHAT_WEBHOOK_HR=
CHAT_WEBHOOK_IT=
CHAT_WEBHOOK_GA=
CHAT_WEBHOOK_LEGAL=

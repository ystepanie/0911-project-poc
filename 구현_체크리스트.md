# 문의 라우팅 자동화 — 구현 체크리스트

> 참고 문서: [업무자동화_PoC_세부구현계획.md](업무자동화_PoC_세부구현계획.md) · [문의게시판_만족도조사_프론트_구현계획.md](문의게시판_만족도조사_프론트_구현계획.md) · [문의라우팅_백엔드_구현계획.md](문의라우팅_백엔드_구현계획.md)
>
> **진행 방식**: 각 단계를 구현한 뒤 사용자 검증을 받고, 승인되면 체크(`[x]`)하고 다음 단계로 진행한다. 체크되지 않은 항목은 아직 검증 전 상태로 간주한다.

---

## Phase 1. 프론트 — 매칭 실패 운영 문구 반영 ✅ (구현·자체 테스트 완료, 사용자 검증 대기)

- [x] 1-1. 매칭 실패 시 문의자 안내 문구를 "관련 팀을 찾지 못했습니다. 담당자가 확인 후 다시 안내드리겠습니다." 형태의 **대기 안내**로 수정 (`js/inquiry.js` 실패 분기)
- [x] 1-2. 실패 사유(`no_team_found` / `low_confidence`)에 따라 화면 문구를 다르게 노출 — "자동으로 찾지 못함" vs "가장 가까운 팀은 찾았지만 확신도 낮음"으로 구분 (⚠️ 검증 필요, 아래 안내 참고)

## Phase 2. 프론트 — 설문 트리거 변경 (관리자 확정 + 접속 시 팝업) ✅ (구현·자체 테스트 완료, 사용자 검증 대기)

- [x] 2-1. 문의 데이터 스키마에 `surveyReady`, `surveyReadyAt` 필드 추가 (`js/api.js`)
- [x] 2-2. 문의 작성 화면 하단에 **데모용 임시 버튼** 추가 — 완료된 문의 중 `surveyReady`를 켜는 용도(관리자 액션 대체)
- [x] 2-3. 문의 제출 시 `contact` 값을 `localStorage`에 `myContact`로 저장
- [x] 2-4. 게시판(`index.html`) 로드 시 `myContact` 기준으로 `surveyReady === true && 미응답`인 문의 조회 → 있으면 **모달 팝업**으로 설문 노출 (`getPendingSurvey`)
- [x] 2-5. 기존 "완료 처리 시뮬레이션" 버튼 클릭 시 `survey.html`로 이동하던 흐름 제거 → 클릭 시 완료 안내 문구만 표시 (⚠️ 검증 필요, 아래 안내 참고)
- [x] 2-6. 설문 제출 UI를 `survey.html` 별도 페이지에서 **`index.html` 내 모달**로 전환 (`survey.html`은 직접 링크 접근 경로로 유지, 미변경)

## Phase 3. 프론트 — 관리자 데모 화면 (최소 기능) ✅ (구현·자체 테스트 완료, 사용자 검증 대기)

- [x] 3-1. "완료됐지만 설문 미발송" 목록을 보여주는 임시 섹션 추가 (정식 관리자 페이지 아님, 흐름 검증용)
- [x] 3-2. "매칭 실패(미배정) 건" 목록 + 수동 팀 배정 데모 UI 추가

## Phase 4. 백엔드 — DB 스키마 ✅ (작성 완료, 실제 DB 적용 검증 필요)

- [x] 4-1. PostgreSQL 마이그레이션 작성: `teams`, `inquiries`(candidate_teams, matcher_mode, survey_ready, assigned_manually 등 포함), `surveys`, `match_audit_log` (`backend/migrations/001~004`)
- [x] 4-2. `teams` 시드 데이터 입력 (`inquiry-board/data/teams.json`과 동일 키워드로 시작, `backend/seeds/001_teams.sql`) — ⚠️ `chat_space_id`는 Phase 6 전까지 placeholder 값

> ⚠️ 이 환경에는 PostgreSQL이 설치되어 있지 않아 `npm run migrate` / `npm run seed` 실제 실행 검증은 하지 못했습니다. 실제 DB에 적용해보고 문제가 없는지 확인이 필요합니다.

## Phase 5. 백엔드 — 팀 매칭 파이프라인 (부분 완료: 로직만 검증, DB/API 연동은 미완)

- [x] 5-1. `CandidateFilter` / `LlmReranker` 인터페이스 정의 (`backend/src/matching/`)
- [x] 5-2. `KeywordCandidateFilter` 구현 (상위 3~5개 후보 추출, 0개 시 `no_candidate` 처리) — `backend/src/matching/candidateFilter.js`, `node scripts/test-matching.js`로 DB 없이 결과 확인 완료
- [x] 5-3. `MockLlmReranker` 구현 (실제 LLM 호출 없이 결정적 규칙으로 confidence 산출, `matcher_mode = 'llm_mock'` 기록) — `backend/src/matching/llmReranker.js`
- [x] 5-4. `POST /api/inquiries` — 등록 + 매칭 파이프라인 실행 (임시 인메모리 저장소 사용, `backend/src/store/inMemoryStore.js`) + `GET /api/inquiries/:id` 함께 구현, curl로 매칭 성공/실패/검증 오류 케이스 확인 완료 (⚠️ 서버 재시작 시 데이터 유실 — DB 연동 전까지의 임시 상태)

## Phase 6. 백엔드 — Google Chat 연동 ✅ (Mock으로 대체, 실 연동은 후속 승인 필요)

- [x] 6-1. Google Chat App 등록, 서비스 계정/권한 설정 — ⚠️ 보류: 사내 Workspace 관리자 협업이 필요해 이 환경에서는 불가. 대신 `MockChatClient`로 대체하고, 동일 인터페이스(`sendMessage`)의 `LiveChatClient`로 교체하면 되도록 구조만 마련
- [x] 6-2. 팀별 전용 스페이스 생성 및 `teams.chat_space_id` 매핑 — placeholder 값(`spaces/PLACEHOLDER_*`)으로 매핑, 실제 스페이스 생성 시 교체 필요
- [x] 6-3. 매칭 성공 시 팀 스페이스로 메시지 전송 로직 구현 — `backend/src/chat/mockChatClient.js`, `POST /api/inquiries`에서 매칭 성공 시 호출, `GET /api/mock-chat/log`로 전송 로그 확인 가능 (curl로 성공/실패 케이스 모두 검증 완료)

## Phase 7. 백엔드 — 완료 트리거 및 무응답 처리 ✅ (⚠️ 타임아웃 값은 PoC 테스트용 5초, 운영값 아님)

- [x] 7-1. `/api/chat/events` — 리액션(✅)/슬래시 명령 수신 → `completed_at`, `completed_by` 기록. 실제 Google Chat App이 없어 `{ inquiryId, actorEmail }` 형태의 단순화된 Mock 페이로드로 대체 (`backend/src/routes/chatEvents.js`). 매칭 실패 건 완료 시도 시 409 반환 확인, 이미 완료된 건은 멱등 처리 확인.
- [x] 7-2. 무응답 타임아웃 스케줄러 (`backend/src/scheduler/timeoutScheduler.js`) — ⚠️ **`backend/src/config.js`에 REMINDER_TIMEOUT_MS/ESCALATION_TIMEOUT_MS/SCHEDULER_INTERVAL_MS를 임시로 5초/5초/1초로 설정해 빠르게 검증함. 실제 운영값(근무시간 기준 2시간 / 2시간 / 15분 주기)이 아니므로 운영 배포 전 반드시 교체 필요** (config.js 상단 주석에도 명시해둠). curl로 리마인드 발송(`reminderSentAt`) → 에스컬레이션(`escalatedAt`) 순서로 정상 전이 확인, 완료된 건은 타임아웃 대상에서 제외되는 것도 확인.

> 참고: 테스트 중 문의 등록과 완료 처리 curl 호출 사이의 실제 소요 시간이 5초에 근접하면서, 완료 처리 직전에 리마인드가 먼저 발송되는 경계 케이스를 관찰했습니다. 로직 버그가 아니라 "타임아웃 임박 시점에 완료 처리가 들어오면 리마인드와 경합할 수 있다"는 실제 운영에서도 나타날 수 있는 자연스러운 동작입니다.

## Phase 8. 백엔드 — 매칭 실패 운영 및 설문 발송 확정 ✅ (⚠️ SLA 값도 PoC 테스트용 5초, 운영값 아님)

- [x] 8-1. `GET /api/admin/unassigned` — 매칭 실패 후 미배정 목록, `slaExceeded` 플래그 포함 (`backend/src/config.js`의 `ADMIN_ASSIGN_SLA_MS`도 Phase 7과 동일하게 **임시 5초**, 운영값은 1영업일 — curl로 5초 전/후 `slaExceeded: false → true` 전환 확인)
- [x] 8-2. `POST /api/admin/inquiries/:id/assign-team` — 관리자 수동 팀 배정 → `sendInquiryToTeam` 공용 로직 재사용해 Chat 전송까지 재진입 확인, 배정 후 `unassigned` 목록에서 제외되는 것 확인
- [x] 8-3. `GET /api/admin/survey-pending` — 완료됐지만 설문 미발송 목록
- [x] 8-4. `POST /api/inquiries/:id/survey-ready` — 관리자 설문 발송 확정 (완료 전 문의에 대해서는 409로 거부)
- [x] 8-5. `GET /api/inquiries/pending-survey?contact={email}`, `POST /api/inquiries/:id/survey`, `GET /api/inquiries/:id` — 확정 전 null → 확정 후 노출 → 설문 제출 후 다시 null로 돌아오는 전체 사이클 curl로 확인

## Phase 9. 백엔드 — 문의자 알림 (이메일 발송, Mock) [신규 삽입] ✅ (⚠️ 9-2, 9-3 방식이 이후 변경됨 — 아래 참고)

- [x] 9-1. `MockEmailClient` 구현 (`backend/src/email/mockEmailClient.js`) — 실제 Gmail API 호출 없이 인메모리 로그 + 콘솔 출력, `sendEmail(to, subject, body)` 인터페이스로 실 연동 시 `LiveEmailClient`로 교체 가능하게 구성. 도메인 문구 조립은 `notifyInquirer.js`로 분리
- [x] ~~9-2. 매칭 성공 시 이메일 발송~~ → **제거**: 문의 결과가 웹페이지 응답으로 바로 표시되므로 제출 시점 이메일은 불필요하다는 피드백 반영
- [x] ~~9-3. 매칭 실패 시 이메일 발송~~ → **제거**: 같은 이유로 실패 직후에도 이메일 발송 안 함
- [x] 9-2'. (수정) **관리자가 매칭 실패 건을 수동 배정 완료했을 때만** 이메일 발송 — `routes/admin.js`의 `assign-team`에서 `notifyTeamAssigned` 호출. curl로 자동 성공/실패 직후엔 로그 비어있고, 수동 배정 후에만 1건 기록되는 것 확인
- [x] 9-4. `GET /api/mock-email/log` — 발송 로그 확인용 디버그 엔드포인트 (Phase 6의 `mock-chat/log`와 동일 패턴)

> ⚠️ 실제 Gmail API 연동은 Google Chat App(Phase 6)과 마찬가지로 사내 Workspace 관리자의 OAuth/서비스 계정 승인이 필요해 이번 PoC 환경에서는 Mock으로 대체한다. 실 연동은 Phase 13(후속 확장 과제, 13-4)로 이동.

## Phase 10. 프론트-백엔드 연동 (기존 Phase 9에서 번호 이동) ✅

- [x] 10-1. `inquiry-board/js/api.js`의 각 mock 함수를 실제 엔드포인트 fetch 호출로 교체 — `mock-data.js`(localStorage 기반 mock)는 더 이상 쓰이지 않아 삭제. `assignTeam`은 팀 이름 대신 `teamId`를 쓰도록 계약이 바뀌어 `admin-demo.js`도 함께 수정 (팀 목록은 신규 `GET /api/teams`로 조회). CORS 허용을 위해 백엔드에 `cors` 미들웨어 추가.
- [x] 10-2. 전체 흐름 통합 테스트 — 브라우저(프론트, `python -m http.server`) + 백엔드(`node src/index.js`) 동시 기동 후 실제 fetch로: 문의 등록(회계팀 매칭 63%) → 완료 처리(`/api/chat/events`) → 관리자 데모에서 설문 발송 확정 → 재접속 시 팝업 노출 → 설문 제출까지 DB(인메모리) 값으로 확인. 매칭 실패 건도 관리자 데모의 실제 팀 드롭다운(`GET /api/teams`)으로 수동 배정해 `unassigned`에서 빠지는 것까지 확인.

> 🐛 **연동 중 발견해 수정한 버그**: `inquiry.js`가 여전히 구버전 매칭 실패 사유(`no_team_found`)를 기준으로 문구를 분기하고 있었는데, Phase 5에서 백엔드가 2단계 파이프라인으로 바뀌며 실패 사유가 `no_candidate`로 변경된 걸 프론트에 반영하지 않은 상태였습니다. 그 결과 "가장 가까운 팀(추정: null)은 찾았지만..." 같은 깨진 문구가 떴던 것을 `no_candidate` 기준으로 수정했습니다.

## Phase 11. 문의 이미지 첨부 지원 [신규 삽입] ✅

오류 메시지 스크린샷 등 이미지로 문의하는 경우를 위한 첨부 기능. **이미지는 팀 전달용으로만 사용하고, 매칭 로직(키워드/LLM 재판단)에는 관여시키지 않는다** — 이미지 내용 자동 인식(OCR/비전)은 복잡도가 높아 후속 과제(13절)로 분리.

- [x] 11-1. 프론트: 문의 작성 화면에 이미지 업로드 필드(선택, 1장) 추가 (`index.html`)
- [x] 11-2. 백엔드: `POST /api/inquiries`가 `multipart/form-data`를 받아 이미지를 저장(`backend/uploads/`)하고 `imageUrl`을 문의 데이터에 기록 — `multer` 사용 (`backend/src/upload/uploadMiddleware.js`), 이미지 없이 JSON으로 보내는 기존 요청도 그대로 통과하도록 구성. 매칭 파이프라인 입력(제목+내용 텍스트)에는 영향 없음을 curl로 확인
- [x] 11-3. Mock Chat 전송 메시지에 첨부 이미지 유무/링크 표시 (`mockChatClient.js`) — curl로 "첨부 이미지: /uploads/..." 줄 포함 확인
- [x] 11-4. 민감정보 안내 문구에 "스크린샷도 팀 전체에 노출됩니다" 문구 추가
- [x] 11-5. 문의 상세 조회 시 이미지 URL 함께 반환, 프론트에서 링크로 노출 (`resolveUploadUrl`) — `/uploads` 정적 서빙 추가, 브라우저에서 이미지 없이 제출하는 기존 흐름도 회귀 없이 정상 동작 확인

> 참고: curl 테스트 중 한글 필드 값을 커맨드라인 인자로 바로 넘기면 이 환경 인코딩 때문에 깨지는 현상(Phase 5 때와 동일)을 다시 겪어서, `content` 필드는 UTF-8 파일에서 읽어 `-F "content=<file"`로 전달해 우회 검증했습니다. 서버 로직 자체의 문제는 아닙니다.

## Phase 12. 파일럿 실행 (기존 Phase 11에서 번호 이동)

- [ ] 12-1. 1~2개 팀 대상 소프트 런칭
- [ ] 12-2. 매칭 실패율, 무응답 SLA 경과 건수, 팝업 노출/응답률 모니터링
- [ ] 12-3. 50~100건 데이터 축적 후 1차 매칭 정확도 산출 (4절)

## Phase 13. (후속, 별도 승인 필요) 확장 과제 (기존 Phase 12에서 번호 이동)

- [ ] 13-1. 사내 구글 SSO 로그인 연동 → `contact`(이메일) 기반 식별을 로그인 세션으로 교체
- [ ] 13-2. 실제 LLM API 연동 (`LiveLlmReranker`) — 사내 보안/비용 검토 완료 후
- [ ] 13-3. 과거 정답 사례 기반 임베딩/RAG 하이브리드 전환
- [ ] 13-4. 실제 Gmail API 연동 (`LiveEmailClient`) — 사내 Workspace 관리자 승인 후
- [ ] 13-5. 매칭 실패 시 관리자 즉시 알림(push) 및 `low_confidence` 문의자 셀프 선택 — 대기시간 보완 방안 (논의만 완료, 구현 보류)
- [ ] 13-6. 첨부 이미지 내용 자동 인식(OCR/비전 LLM) → 매칭 파이프라인 입력에 포함 (Phase 11에서 의도적으로 제외한 부분)

---

## 부록. Mock/임시 처리/미구현 항목 총정리

지금까지 구현한 것 중 **실제 서비스 배포 전 반드시 다시 봐야 할 항목**을 한곳에 모았다. 각 항목은 해당 Phase에도 개별적으로 표시돼 있으니, 여기서는 "무엇이 진짜가 아닌지"만 빠르게 훑어볼 수 있게 정리한다.

### A. 외부 연동을 Mock으로 대체한 것 (실제 서비스 붙이려면 계정/승인 필요)

| 항목 | 현재 상태 | 실제로 필요한 것 | 관련 Phase |
|---|---|---|---|
| LLM 재판단 | `MockLlmReranker` — 실제 API 호출 없이 키워드 점수로 confidence를 결정적 규칙으로 흉내 (`backend/src/matching/llmReranker.js`) | 실 LLM API 계약, 문의 원문이 외부로 나가는 것에 대한 보안 검토 | 5-3 / 후속 13-2 |
| Google Chat 전송 | `MockChatClient` — 실제 API 대신 콘솔 출력 + 인메모리 로그 (`backend/src/chat/mockChatClient.js`) | Google Chat App 등록, 서비스 계정, 팀별 실제 스페이스 생성 | 6-1~6-3 |
| 완료 트리거(리액션 웹훅) | `POST /api/chat/events`가 실제 Google 이벤트 대신 단순화된 `{ inquiryId, actorEmail }` 페이로드를 받음 (`backend/src/routes/chatEvents.js`) | Workspace Events API + Cloud Pub/Sub 구독, 이벤트 서명 검증 | 7-1 |
| 이메일 발송 | `MockEmailClient` — 실제 Gmail API 대신 콘솔 출력 + 인메모리 로그 (`backend/src/email/mockEmailClient.js`) | Gmail API OAuth/서비스 계정 승인 | 9-1 / 후속 13-4 |
| 사용자 로그인 | 로그인 없음 — 문의 작성 시 입력한 연락처(이메일)를 `localStorage`의 `myContact`로 저장해 임시 식별자로 사용 | 사내 구글 SSO 연동 | 후속 13-1 |
| 팀 배정 관리자 인증 | 관리자 액션(`assign-team`, `survey-ready`, 완료 트리거)에 실제 권한 검사 없음 — 아무나 호출 가능 | 관리자 인증/권한 체계 | 미착수 |

### B. 데이터 저장을 임시로 처리한 것

| 항목 | 현재 상태 | 실제로 필요한 것 | 관련 Phase |
|---|---|---|---|
| 문의/설문 데이터 저장소 | `backend/src/store/inMemoryStore.js` — 서버 프로세스 안 배열에만 저장, **재시작하면 전부 사라짐** | PostgreSQL 연결 + repository 계층으로 교체 | 5-4 / 미착수(구 Phase 9 DB 연동 항목) |
| PostgreSQL 스키마 | 마이그레이션(`backend/migrations/001~004`)과 시드(`backend/seeds/001_teams.sql`)는 작성했지만, 이 환경에 PostgreSQL이 없어 **실행 검증을 못 함** | 실제 DB에 `npm run migrate`/`npm run seed` 실행해 문제 없는지 확인 | 4-1, 4-2 |
| 팀 R&R 데이터 중복 | `backend/seeds/001_teams.sql`(DB용)과 `backend/src/matching/teams-seed.json`(매칭 로직이 실제로 읽는 파일)이 **서로 다른 파일에 같은 내용을 중복 보관** 중 | DB 연동 후에는 매칭 로직이 `teams-seed.json` 대신 실제 `teams` 테이블을 조회하도록 교체 | 5-4 |
| Chat 스페이스 ID | `teams-seed.json`/시드 SQL 모두 `spaces/PLACEHOLDER_*` 값 | Google Chat App 등록 후 실제 스페이스 ID로 교체 | 6-2 |
| 첨부 이미지 저장소 | `backend/uploads/` 로컬 디스크에 저장 — 서버 재배포/다중 인스턴스 환경에서는 파일 유실 위험, 용량 제한/자동 정리 로직도 없음 | 클라우드 오브젝트 스토리지(S3류) 연동, 용량/보관기간 정책 수립 | 11-2 |

### C. 운영값을 테스트용으로 낮춰놓은 것 (⚠️ 배포 전 반드시 교체)

모두 `backend/src/config.js`에 있고, 파일 상단에도 동일 경고를 남겨뒀다.

| 설정값 | 현재(PoC 테스트) | 실제 운영 목표값 |
|---|---|---|
| `REMINDER_TIMEOUT_MS` (무응답 리마인드) | 5초 | 2시간 (근무시간 기준) |
| `ESCALATION_TIMEOUT_MS` (리마인드 후 에스컬레이션) | 5초 | 2시간 |
| `SCHEDULER_INTERVAL_MS` (타임아웃 체크 주기) | 1초 | 15분 |
| `ADMIN_ASSIGN_SLA_MS` (매칭 실패 수동 배정 SLA) | 5초 | 1영업일 |

### D. 정식 화면 대신 임시 데모로 대체한 것

| 항목 | 현재 상태 |
|---|---|
| 관리자 페이지 | 정식 페이지 없음. `inquiry-board/index.html` 하단 "관리자 데모" 섹션(미배정 목록 + 설문 발송 확정 목록)이 대체 중 — 인증 없이 누구나 접근 가능 |
| 완료 트리거 UI | "완료 처리 시뮬레이션" 버튼 — 실제로는 Google Chat에서 ✅ 리액션으로 발생해야 함 |

### E. 보안/설정상 임시로 느슨하게 둔 것

- 백엔드 CORS가 모든 origin을 허용 (`app.use(cors())`, 옵션 없음) — 운영 배포 시 프론트 도메인으로 제한 필요
- `POST /api/chat/events`의 `actorEmail`, `POST /api/inquiries/:id/survey-ready`의 `adminEmail` 등은 클라이언트가 보낸 값을 그대로 신뢰 — 실제로는 인증된 세션에서 가져와야 함
- 매칭 실패 시 문의 원문이 결국 LLM/사람에게 그대로 노출되는 구조라, 3.3절 민감정보 안내 문구 외의 자동 필터링은 여전히 없음 (PoC 범위 밖으로 원래부터 확정된 사항)

### F. 아직 논의만 하고 구현을 보류한 것

- 매칭 실패 시 관리자에게 즉시 알림(push) — 지금은 관리자가 "관리자 데모" 화면을 직접 열어봐야 알 수 있음 (13-5)
- `low_confidence` 케이스에서 문의자가 후보 팀 중 직접 선택하는 셀프 서비스 옵션 (13-5)
- 개인 단위 정밀 매칭, 임베딩/RAG 하이브리드 전환 (13-3)

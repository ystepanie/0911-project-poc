# 문의게시판 · 만족도조사 팝업 프론트 구현 계획

> 상위 문서: [업무자동화_PoC_세부구현계획.md](업무자동화_PoC_세부구현계획.md) 3.1, 3.3(안내문구), 3.5 절 기반
> 범위: 프론트엔드만 (백엔드는 Mock API로 대체) — 순수 HTML/JS

---

## 1. 목표 및 전제

- 이번 단계는 **백엔드 연동 없이** 프론트 화면과 흐름만 먼저 완성한다.
- 실제 API 나오면 **fetch 호출부만 교체**하면 되도록, 데이터 접근을 별도 모듈(`api.js`)로 분리해 Mock ↔ 실 API 전환 비용을 최소화한다.
- 대상 화면
  1. 문의 작성 화면 (문의게시판)
  2. 문의 제출 완료 화면 (문의 ID 노출)
  3. 만족도조사 팝업 (전반 만족도 + 매칭 정확도 + 자유의견, 3.5절 문항 그대로) — **완료 즉시 자동 발송이 아니라, 관리자가 발송을 확정한 건에 한해 문의게시판 재접속 시 팝업으로 노출** (아래 3.2절 변경 사항 참고)
- 목록/상태조회 화면은 이번 범위 밖 (후속 단계).

---

## 2. 파일/폴더 구조

```
/inquiry-board
  index.html          # 문의 작성 화면
  survey.html          # 만족도조사 팝업 (모달 or 별도 페이지로 열림)
  /css
    style.css
  /js
    api.js            # Mock API — 나중에 실제 fetch로 교체될 유일한 지점
    mock-data.js       # 팀 목록, 매칭 로직 mock, 문의 저장소(localStorage)
    inquiry.js         # 문의 작성 화면 로직
    survey.js          # 설문 팝업 로직
  /data
    teams.json         # 팀별 R&R 러프 문서 (3.2절) — 매칭 mock에 사용
```

---

## 3. 화면별 상세 설계

### 3.1 문의 작성 화면 (`index.html` / `inquiry.js`)

**구성 요소**
- 문의 제목 입력
- 문의 내용 textarea
- 작성자 정보 (임시로 이름/이메일 입력 또는 로그인 mock)
- **민감정보 안내 문구** (3.3절): textarea 상단 또는 바로 아래 고정 노출
  - 예: "⚠️ 해당 문의사항은 유관 부서 팀 전체에 전달됩니다. 급여, 인사평가, 법적 분쟁 등 민감정보가 포함된 문의는 담당자 지정 시 추후 문의해주세요."
- "확인" 버튼 (제출)

**동작 흐름**
1. 사용자가 입력 후 "확인" 클릭
2. 클라이언트 유효성 검사 (제목/내용 필수)
3. `api.submitInquiry(payload)` 호출 (Mock: `mock-data.js`에서 처리)
4. Mock 내부에서:
   - 문의 ID 발급 (`Q-0001` 형식, localStorage 시퀀스로 증가)
   - `teams.json` 기반 **키워드 매칭 mock** 수행 → 매칭된 팀 + 신뢰도 점수(mock은 랜덤/규칙 기반) 반환
   - 신뢰도 임계값 미만이면 `matchFailed: true` + 실패 사유 카테고리(`no_team_found` / `low_confidence`) 반환
5. 결과 화면 분기:
   - 매칭 성공 → "문의가 OO팀으로 전달되었습니다 (문의 ID: Q-0001)" 안내 + Google Chat 전송 mock 로그(콘솔 or 화면에 "전송됨" 표시)
   - 매칭 실패 → "관련 팀을 찾지 못했습니다. 관리자 페이지로 전달됩니다." 안내 (3.2절)
6. 문의 데이터는 localStorage에 축적 (`inquiries` 배열) — 문의 ID, 작성자, 원문, 매칭팀, 신뢰도, 실패여부, 작성시각 저장 (3.1절 최소 저장 항목 반영)

**엣지 케이스**
- 빈 제목/내용 제출 방지
- 매칭 실패 케이스 UI 별도 확인 (성공 케이스만 만들고 끝내지 않기)

---

### 3.2 만족도조사 팝업 (`survey.html` / `survey.js`)

**트리거 방식 (변경: 완료 즉시 자동 발송 → 관리자 확정 + 접속 시 팝업)**
- 완료 트리거(팀원 ✅)가 발생해도 설문이 자동으로 나가지 않는다. **관리자가 별도로 "설문 발송" 확정 액션을 해야** 해당 문의가 발송 대상(`surveyReady: true`)이 된다. (배정 시점의 ✅와 실제 해결 여부가 다를 수 있어 조기 발송을 막기 위함)
- 발송 방식은 이메일/챗 링크(push)가 아니라, **문의게시판(`index.html`) 재접속 시 팝업으로 노출**(pull)하는 방식으로 한다.
- **사용자 식별**: 로그인(사내 구글 SSO)은 후속 과제이므로, 지금은 문의 작성 시 입력한 **연락처(이메일)를 임시 식별자**로 사용한다.
  - 문의 제출 시 입력한 `contact` 값을 `localStorage`에 `myContact`로 저장해두고, 게시판 접속 시 이 값과 일치하면서 `surveyReady: true`이고 아직 응답하지 않은 문의가 있는지 확인해 팝업을 띄운다.
  - SSO 연동 후에는 `myContact`를 로그인 세션 이메일로 대체하기만 하면 동일 로직을 재사용할 수 있도록, 필드명을 이메일 기준으로 통일해둔다.
- Mock 단계에서 관리자의 "설문 발송 확정" 액션은 아직 관리자 페이지가 없으므로, 문의 작성 화면 하단에 **데모용 임시 버튼**(완료된 문의 중 `surveyReady`를 켜는 용도)으로 대체해 흐름을 테스트한다. 실제 관리자 페이지는 후속 범위(7절).

**문항 구성 (3.5절 표 그대로)**
| 구분 | UI 요소 |
|---|---|
| 전반 만족도 | 1~5점 라디오 또는 별점 |
| 매칭 정확도 | "처음 연결된 팀이 올바른 담당 팀이었나요?" 예/아니오 라디오 |
| 자유 의견 | textarea (선택 입력) |

**동작 흐름**
1. 문의 ID를 쿼리 파라미터 또는 모달 호출 시 전달받음
2. 제출 시 `api.submitSurvey(inquiryId, { satisfaction, matchCorrect, comment })` 호출
3. Mock에서 해당 문의 레코드에 설문 결과 병합 저장 (localStorage)
4. 제출 완료 후 "감사합니다" 메시지 + 팝업 닫기

**UI 형태**
- 게시판 접속 시 노출되는 팝업이므로 **`index.html` 내 모달 오버레이**로 구현한다 (별도 페이지 이동이 아니라, 접속 즉시 화면 위에 뜨는 형태가 "팝업" 요구사항에 더 부합). `survey.html`은 직접 링크로 접근하는 경로(예: 관리자가 별도로 링크를 공유하는 경우)로 계속 유지하되, 주 경로는 팝업으로 전환한다.

---

## 4. Mock API 설계 (`api.js`)

```js
// 실제 API 붙일 때 이 파일의 함수 내부만 fetch로 교체
export async function submitInquiry(payload) { ... }   // → 문의 ID, 매칭결과 반환
export async function submitSurvey(inquiryId, answers) { ... }
export async function getInquiry(inquiryId) { ... }     // 설문 화면에서 문의 요약 표시용
export async function getPendingSurvey(contact) { ... }  // contact 기준 surveyReady && 미응답 문의 조회 (팝업 노출 판단용)
export async function markSurveyReady(inquiryId) { ... } // 관리자 확정 액션 (Mock: 데모 버튼에서 호출)
```

- 지연 시뮬레이션을 위해 `setTimeout` 또는 `Promise` 기반으로 실제 네트워크 호출과 동일한 인터페이스 유지
- 응답 스키마는 문서 3.1절 저장 항목을 그대로 필드명으로 사용 (추후 백엔드 스펙 협의 시 그대로 매핑되도록)

---

## 5. 데이터 스키마 (localStorage 기준, 3.1절 반영)

```json
{
  "id": "Q-0001",
  "author": "홍길동",
  "contact": "hong@example.com",
  "title": "...",
  "content": "...",
  "createdAt": "2026-09-11T10:00:00+09:00",
  "matchedTeam": "회계팀",
  "matchConfidence": 0.82,
  "matchFailed": false,
  "failReason": null,
  "chatSentAt": null,
  "completedAt": null,
  "surveyReady": false,
  "surveyReadyAt": null,
  "survey": {
    "satisfaction": null,
    "matchCorrect": null,
    "comment": null,
    "answeredAt": null
  }
}
```

---

## 6. 작업 순서 (체크리스트)

1. 폴더/파일 스캐폴딩 + `teams.json` mock 데이터 3~5개 팀 작성
2. `api.js`, `mock-data.js` — 매칭 로직 mock, localStorage read/write
3. 문의 작성 화면 (`index.html`, `inquiry.js`) — 폼 + 민감정보 안내문구 + 제출 처리
4. 매칭 성공/실패 분기 UI
5. 만족도조사 페이지/모달 (`survey.html` 및 `index.html` 내 모달) — 문항 3종 + 제출
6. 완료 트리거 시뮬레이션 버튼 + "설문 발송 확정"(관리자 데모) 버튼으로 `surveyReady` 전환 흐름 구성
7. 게시판 접속 시 `myContact` 기준 팝업 노출 로직 (`getPendingSurvey`) 구현
8. 브라우저에서 전체 흐름 수동 테스트 (작성 → 매칭 → 완료 → 설문 발송 확정 → 재접속 시 팝업 → 설문 제출 → localStorage 확인)

---

## 7. 이번 범위에서 제외 (documented as 보류/후속)

- Google Chat 실전 연동, 무응답 타임아웃/리마인드 로직 (백엔드/스케줄러 영역)
- 정식 관리자 페이지, 목록/상태 조회 화면 (이번 프론트에서는 데모용 임시 버튼으로만 흐름 검증)
- 사내 구글 SSO 로그인 연동 (그 전까지는 연락처(이메일)를 임시 식별자로 사용)
- 실제 민감정보 자동 필터링 (안내 문구만 노출)
- 매칭 정확도 실제 산출 로직 (RAG는 mock 키워드 매칭으로 대체)

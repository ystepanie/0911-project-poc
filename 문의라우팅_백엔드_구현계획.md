# 문의 라우팅 자동화 — 백엔드 구현 계획

> 상위 문서: [업무자동화_PoC_세부구현계획.md](업무자동화_PoC_세부구현계획.md)
> 프론트 연계: [문의게시판_만족도조사_프론트_구현계획.md](문의게시판_만족도조사_프론트_구현계획.md), [inquiry-board/js/api.js](inquiry-board/js/api.js)
> 범위: Phase 1 (개발 및 파일럿) — 문의 접수 → 팀 매칭 → Google Chat 전송 → 완료 트리거 → 관리자 확정 후 설문 팝업 노출, 데이터 축적(4절)

---

## 1. 기술 스택 권장안

| 영역 | 선택 | 근거 |
|---|---|---|
| 런타임/프레임워크 | **Node.js + Express** | 프론트(`inquiry-board`)와 동일 언어로 팀 온보딩 비용 최소화, Google Chat API(`googleapis` 공식 SDK)와 Webhook 처리가 Node 생태계에서 수월 |
| DB | **PostgreSQL** | 문의/매칭/설문의 관계형 데이터에 적합. 이번 단계는 키워드 매칭이라 불필요하지만, 2단계(4.3절)에서 RAG 전환 시 `pgvector` 확장만 추가하면 되므로 마이그레이션 부담이 없음 |
| 스케줄러 | **node-cron** (또는 저부하이므로 OS cron + 스크립트) | 무응답 타임아웃 체크(3.4절)용, 별도 큐 시스템 없이 파일럿 규모(50~100건)에서 충분 |
| Google Chat 연동 | **Chat Bot (App) + Webhook 이벤트 구독** | 팀별 스페이스 전송(발신)과 이모지 리액션 수신(완료 트리거) 둘 다 필요하므로 단순 Incoming Webhook만으로는 부족 — Chat App 등록 필요 (2절 참고) |
| 배포 | 사내 인트라넷 서버 or 사내 클라우드(파일럿 규모이므로 별도 인프라 신설 최소화) | 1~2개 팀 대상 소프트 런칭이므로 과설계 지양 |

**매칭 로직**: **키워드 규칙 매칭으로 후보군 3~5개를 추린 뒤, LLM API로 최종 재판단**하는 2단계 구조로 구현한다.
- 1단계(후보 추출): 프론트 mock과 동일한 키워드 겹침 방식으로 팀별 점수를 매겨 상위 3~5개 후보 팀을 추림 (임계값 미달로 후보가 0개면 `no_team_found`로 즉시 실패 처리, 5.6절 참고).
- 2단계(재판단): 후보 팀들의 R&R 설명과 문의 원문을 LLM에 함께 제공해 최종 팀과 확신도(confidence)를 구조화 출력(JSON)으로 받음. 후보가 1개뿐이어도 재판단 단계를 거쳐 confidence를 산출한다.
- **PoC 단계에서는 이 LLM 재판단을 실제 API 호출 없이 Mock으로 구현한다.** 실제 배포용 LLM 계약/보안 검토(문의 원문이 외부로 전송되는 문제, 3.3절 민감정보 리스크와 직결)가 끝나기 전까지, `LlmReranker` 인터페이스의 Mock 구현체가 후보 중 키워드 점수 1위를 그대로 반환하거나 규칙 기반의 가짜 confidence를 부여하는 방식으로 동작을 흉내낸다. 실제 LLM 연동은 별도 승인 후 `LlmReranker` 구현체만 교체하면 되도록 인터페이스를 분리한다(5.1절).
- 이후 4.3절(데이터 축적)에 따라 과거 정답 사례가 쌓이면, 이 사례들을 임베딩 인덱스에 넣어 LLM 프롬프트에 few-shot으로 주입하는 하이브리드(RAG)로 확장할 수 있다.

---

## 2. 시스템 아키텍처 개요

```
[문의게시판 프론트]
      │ POST /api/inquiries
      ▼
[Backend API Server]
      │  1) 키워드 규칙으로 후보 팀 3~5개 추출 (KeywordCandidateFilter)
      │  2) 후보 중 LLM 재판단으로 최종 팀+confidence 산출 (LlmReranker, PoC 단계는 Mock)
      ▼
   [PostgreSQL] (inquiries, teams, match_logs, surveys)
      │ 매칭 성공 시
      ▼
[Google Chat App] → 팀 스페이스에 메시지 전송 (문의 ID, 내용, 문의자)
      │
      │ (팀원이 ✅ 리액션 또는 /complete 슬래시 명령)
      ▼
[Chat 이벤트 Webhook 수신] → 완료 처리 (completed_at 기록, 설문 자동 발송 없음)
      │
      ▼
[관리자] 완료 건 목록에서 "설문 발송" 확정 → survey_ready = true
      │
      ▼
[문의게시판 접속 시] GET /api/inquiries/pending-survey?contact={email}
      │ 결과 있으면 팝업으로 만족도 설문 노출
      ▼
[만족도 설문 팝업 제출] POST /api/inquiries/:id/survey
      ▼
[PostgreSQL 저장] → 4절 데이터 축적/분석

[스케줄러] 2시간 무응답 감지 → 팀장 멘션 리마인드 → 계속 무응답 시 관리자 페이지 노출
```

> 문의자 식별은 현재 연락처(이메일) 기준 임시 방식이며, 추후 사내 구글 SSO 연동 시 로그인 세션 이메일로 그대로 대체 가능하도록 필드를 이메일 기준으로 통일해둔다 (5.5절).

---

## 3. 데이터베이스 스키마 (PostgreSQL)

### 3.1 `teams` — 팀 R&R 문서 (3.2절)

```sql
CREATE TABLE teams (
  id            SERIAL PRIMARY KEY,
  name          VARCHAR(100) NOT NULL UNIQUE,
  chat_space_id VARCHAR(200) NOT NULL,   -- Google Chat space ID
  keywords      TEXT[] NOT NULL,          -- 키워드 규칙 매칭용 (2단계에서 embedding 컬럼 추가 예정)
  description   TEXT,
  created_at    TIMESTAMPTZ DEFAULT now(),
  updated_at    TIMESTAMPTZ DEFAULT now()
);
```

### 3.2 `inquiries` — 문의 (3.1절 저장 항목 그대로)

```sql
CREATE TABLE inquiries (
  id              VARCHAR(20) PRIMARY KEY,       -- 'Q-0001' 형식
  author_name     VARCHAR(100) NOT NULL,
  author_email    VARCHAR(200) NOT NULL,
  title           VARCHAR(300) NOT NULL,
  content         TEXT NOT NULL,
  created_at      TIMESTAMPTZ DEFAULT now(),

  candidate_teams JSONB,                          -- 1단계 키워드 후보군 3~5개 + 각 점수 (감사/디버깅용)
  matched_team_id INTEGER REFERENCES teams(id),    -- 2단계 LLM 재판단 결과
  match_confidence NUMERIC(4,3),
  match_failed    BOOLEAN DEFAULT false,
  fail_reason     VARCHAR(30),                    -- 'no_candidate' | 'no_team_found' | 'low_confidence'
  matcher_mode    VARCHAR(20) DEFAULT 'llm_mock',  -- 'llm_mock' | 'llm_live' — 어떤 재판단기가 처리했는지 기록

  chat_message_id VARCHAR(200),                   -- Chat 메시지 링크/ID
  chat_sent_at    TIMESTAMPTZ,

  completed_at    TIMESTAMPTZ,
  completed_by    VARCHAR(200),                    -- 완료 처리한 사용자(리액션 누른 사람)

  reminder_sent_at TIMESTAMPTZ,                    -- 무응답 리마인드 발송 시각
  escalated_at     TIMESTAMPTZ,                     -- 관리자 페이지 노출 시각

  survey_ready     BOOLEAN DEFAULT false,           -- 관리자가 설문 발송을 확정했는지
  survey_ready_at  TIMESTAMPTZ,
  survey_ready_by  VARCHAR(200)                     -- 발송 확정한 관리자
);

CREATE INDEX idx_inquiries_matched_team ON inquiries(matched_team_id);
CREATE INDEX idx_inquiries_match_failed ON inquiries(match_failed);
```

### 3.3 `surveys` — 만족도 조사 (3.5절)

```sql
CREATE TABLE surveys (
  inquiry_id      VARCHAR(20) PRIMARY KEY REFERENCES inquiries(id),
  satisfaction    SMALLINT CHECK (satisfaction BETWEEN 1 AND 5),
  match_correct   BOOLEAN,          -- 4.1절 골드 라벨의 핵심 필드
  comment         TEXT,
  answered_at     TIMESTAMPTZ
);
```

### 3.4 `match_audit_log` (선택, 4.2절 편향 보정용)

```sql
CREATE TABLE match_audit_log (
  id           SERIAL PRIMARY KEY,
  inquiry_id   VARCHAR(20) REFERENCES inquiries(id),
  audited_by   VARCHAR(200),
  audit_result BOOLEAN,             -- 수동 감사 결과: 실제 매칭이 맞았는지
  note         TEXT,
  audited_at   TIMESTAMPTZ DEFAULT now()
);
```

---

## 4. API 설계

| Method | Endpoint | 설명 | 프론트 연동 지점 |
|---|---|---|---|
| POST | `/api/inquiries` | 문의 등록 → 매칭 수행 → (성공 시) Chat 전송 | `api.js: submitInquiry` |
| GET | `/api/inquiries/:id` | 문의 요약 조회 (설문 화면에서 제목 표시용) | `api.js: getInquiry` |
| POST | `/api/inquiries/:id/complete` | 완료 처리 (Chat 리액션 웹훅에서도 내부적으로 이 로직 재사용) | `api.js: completeInquiry` |
| POST | `/api/inquiries/:id/survey` | 설문 제출 | `api.js: submitSurvey` |
| GET | `/api/inquiries/pending-survey?contact={email}` | 해당 문의자(이메일 기준) 명의로 `survey_ready=true`이고 미응답인 문의 조회 → 게시판 접속 시 팝업 노출 판단 | 게시판 접속 시 폴링 |
| POST | `/api/inquiries/:id/survey-ready` | 관리자가 설문 발송 확정 (자동 발송 대신 수동 확정) | 관리자 페이지(후속) |
| POST | `/api/chat/events` | Google Chat 이벤트 수신 웹훅 (리액션/슬래시 명령) | 내부 전용 |
| GET | `/api/teams` | 팀 목록/키워드 조회 (관리자용) | 관리자 페이지(후속) |
| GET | `/api/admin/escalated` | 매칭 실패·무응답 건 목록 | 관리자 페이지(후속) |
| GET | `/api/admin/survey-pending` | 완료됐지만 설문 미발송(`survey_ready=false`)인 건 목록 | 관리자 페이지(후속) |

**요청/응답 스키마는 현재 프론트 mock(`inquiry.js`, `survey.js`, `mock-data.js`)의 필드명과 동일하게 맞춰, `api.js`의 fetch 교체만으로 연동 가능하도록 한다.**

---

## 5. 핵심 로직 상세

### 5.1 팀 매칭 파이프라인 (키워드 후보 추출 + LLM 재판단)

두 단계를 각각 별도 인터페이스로 분리해, 재판단기만 교체(Mock → 실제 LLM → 향후 RAG)할 수 있도록 한다.

```
interface CandidateFilter {
  getCandidates(text: string): Array<{ teamId, teamName, keywordScore }>   // 상위 3~5개
}

interface LlmReranker {
  rerank(text: string, candidates: Candidate[]): { teamId, teamName, confidence, reason }
}
```

**1단계: `KeywordCandidateFilter`**
- 프론트 mock 로직(`mock-data.js: matchTeam`)과 동일한 키워드 겹침 방식으로 전 팀 점수를 매기고, 점수 상위 3~5개(0점 제외)를 후보로 추림
- 후보가 0개면 즉시 `match_failed = true`, `fail_reason = 'no_candidate'`로 종료 (LLM 호출 자체를 생략 — 불필요한 호출/비용 방지)

**2단계: `LlmReranker`**
- 후보 팀들의 R&R 설명(`teams.description`)과 문의 원문을 프롬프트로 구성해 LLM에 전달, 다음 스키마의 구조화 출력(JSON)을 강제:
  ```json
  { "teamId": 3, "confidence": 0.82, "reason": "문의 내용이 법인카드/세금계산서 관련으로 회계팀 업무와 직접 일치" }
  ```
- confidence가 임계값(`MATCH_THRESHOLD`) 미만이면 `match_failed = true`, `fail_reason = 'low_confidence'`
- **PoC 단계 구현: `MockLlmReranker`**
  - 실제 LLM API를 호출하지 않고, 후보 중 `keywordScore`가 가장 높은 팀을 그대로 채택
  - confidence는 `keywordScore`를 정규화하거나 고정 규칙(예: 1위-2위 점수 차이가 크면 0.8~0.9, 근소하면 0.5~0.6)으로 산출하는 결정적 함수로 흉내냄 — 실제 LLM 호출과 동일한 인터페이스/응답 스키마를 지키는 것이 핵심
  - `inquiries.matcher_mode = 'llm_mock'`으로 기록해, 추후 실제 LLM 도입 후 정확도를 구간별로 비교 분석할 수 있게 함
- **실 LLM 전환 시**: 사내 승인된 LLM 엔드포인트(3.3절 민감정보 검토 완료 후) 기준으로 `LiveLlmReranker` 구현체만 추가하고 설정으로 스위칭 — API 레이어/DB 스키마 변경 불필요
- 향후 `EmbeddingLlmReranker`(과거 사례를 few-shot으로 주입하는 RAG 방식)로 교체할 때도 동일 인터페이스 유지

### 5.2 Google Chat 전송

- 문의 등록 시 매칭 성공하면 해당 팀의 `chat_space_id`로 메시지 전송
  - 메시지 포맷: `[문의 ID] 제목 / 작성자 / 문의 ID / 완료 처리 방법 안내(✅ 리액션 남기기)`
- 신규 전용 스페이스 사용 (3.3절 파일럿 방침)
- 전송 성공 시 `chat_message_id`, `chat_sent_at` 저장

### 5.3 완료 트리거 (3.4절)

- Google Chat App이 **리액션 추가 이벤트**를 구독 (`REACTION_ADDED`) — 지정 이모지(✅)를 처리자로 인식
- 이벤트 수신 시 `/api/chat/events` → 메시지에 매핑된 `inquiry_id` 조회 → `completed_at`, `completed_by` 기록 → 설문 발송 프로세스 트리거
- 슬래시 명령(`/complete`)도 동일 엔드포인트로 처리 (백업 경로)

### 5.4 무응답 타임아웃 / 리마인드 (3.4절)

- 스케줄러(예: 매 15분 실행 cron job)
  - `chat_sent_at`이 N시간(근무시간 기준 2시간) 이상 지났고 `completed_at IS NULL`이고 `reminder_sent_at IS NULL`인 문의 조회
  - 팀장 멘션 메시지를 해당 Chat 스페이스에 추가 전송, `reminder_sent_at` 갱신
  - 리마인드 후에도 추가 N시간 응답 없으면 `escalated_at` 기록 → 관리자 페이지 노출 대상으로 분류
- "근무시간 기준"이므로 계산 시 주말/공휴일 제외 로직 필요 (사내 근무일 캘린더 하드코딩 또는 설정 테이블로 관리)

### 5.5 만족도 설문 발송 (관리자 지정 발송 + 접속 시 팝업 노출)

기존안(완료 트리거 발생 즉시 자동 발송)은 담당자의 ✅가 "배정"인지 "실제 해결"인지 모호해 설문이 조기 발송될 위험이 있다는 논의를 반영해 다음과 같이 변경한다.

- 담당자의 ✅ 완료 트리거(5.3절)는 그대로 유지하되, **설문 발송은 자동이 아니라 관리자(최종 관리자)가 별도로 확정**한다.
  - `completed_at IS NOT NULL AND survey_ready = false`인 문의를 관리자 페이지에서 목록으로 노출
  - 관리자가 "설문 발송" 액션 실행 → `survey_ready = true`, `survey_ready_at`, `survey_ready_by` 기록
- **전달 방식도 푸시(이메일/챗 링크) 대신 풀(pull) 방식으로 변경**: 문의자가 문의게시판에 접속했을 때, 본인 명의의 문의 중 `survey_ready = true` and 미응답 건이 있으면 **팝업 형태로 노출**한다.
  - 이메일/Chat DM 발송 로직이 불필요해져 구현이 단순해짐
  - 단, 문의자가 게시판에 다시 방문하지 않으면 설문에 응답하지 않을 수 있음 → 응답률은 자동 발송 대비 낮아질 수 있음(파일럿 규모에서는 감내 가능한 트레이드오프로 판단)
- **사용자 식별 문제(로그인 미구현 구간)**: 사내 구글 계정 SSO 연동은 후속 과제이며, 그 전까지는 **문의 작성 시 입력한 연락처(이메일)를 임시 식별자**로 사용한다.
  - 문의게시판 접속 시 클라이언트가 보유한 사용자 식별 정보(추후 SSO 세션의 이메일, 현재는 임시로 브라우저에 남겨둔 최근 입력 연락처)로 `GET /api/inquiries/pending-survey?contact={email}` 조회 → 결과가 있으면 팝업 표시
  - SSO 연동 후에는 `contact` 대신 로그인 세션의 이메일로 동일 쿼리를 그대로 재사용 가능하도록 설계 (쿼리 파라미터/필드명을 이메일 기준으로 통일해둠)
- 관리자가 발송 확정을 잊는 리스크에 대비해, 관리자 페이지에 "완료됐지만 설문 미발송" 건수를 상시 노출 (알림/배지 형태)

### 5.6 매칭 실패 처리 및 운영 방안 (3.2절)

- `match_failed = true`인 문의는 즉시 관리자 페이지 대상 목록에 편입 (`escalated_at` 즉시 기록)
- 실패 사유를 3가지로 분리 저장 → 4절 분석 시 recall/precision 문제 구분
  - `no_candidate`: 키워드 단계에서 후보 자체가 없음 (recall 문제 → R&R 키워드 보강 신호)
  - `no_team_found` / `low_confidence`: 후보는 있었으나 LLM 재판단 confidence가 임계값 미달 (precision 문제 → 팀 설명 문서 품질 또는 재판단 로직 개선 신호)

**운영 방안 (실패 이후 처리 흐름)**

1. **문의자 안내**: 실패 즉시 "관련 팀을 찾지 못했습니다. 담당자가 확인 후 다시 안내드리겠습니다" 등 대기 안내를 노출해, 실패를 막연한 종료가 아니라 처리 중 상태로 인지시킨다.
2. **관리자 큐 + SLA**: 매칭 실패 건은 관리자 페이지에 "미배정" 상태로 실시간 노출하고, **수동 배정 SLA(예: 1영업일 이내)**를 둔다. SLA 경과 건은 무응답 타임아웃(3.4절)과 동일하게 별도 알림 대상으로 취급한다.
3. **수동 배정 후 정상 플로우 재진입**: 관리자가 팀을 수동 지정하면(`POST /api/admin/inquiries/:id/assign-team`) 그 시점부터 기존 Chat 전송 → 완료 트리거 → 설문 흐름을 그대로 이어간다. 매칭 실패가 전체 프로세스를 막지 않고, 첫 단계만 사람이 대신하는 구조로 설계한다.
4. **실패 유형별 관리자 UX 차등화**:
   - `no_candidate`: 반복 발생 시 R&R 키워드/문서 자체가 비어있다는 신호이므로, 관리자가 배정하면서 해당 팀 문서에 키워드를 보강할 수 있는 도구(간단한 인라인 편집)를 함께 제공
   - `low_confidence`: 관리자 화면에 **후보 3~5개와 LLM `reason` 필드를 그대로 노출**해, 관리자가 왜 확신이 낮았는지 빠르게 판단하고 팀을 고를 수 있게 한다
5. **캐치올(catch-all) 팀 지정 지양**: "일단 총무팀으로 보내고 거기서 재분배"하는 방식은 쓰지 않는다. PoC 설계 원칙(팀 단위 매칭으로 리스크를 낮추되 최종 판단은 사람에게, 문서 1절)과 충돌하고, 애초에 우회하려던 "오답 반송 로직"을 다시 만드는 셈이다.

**추가 API**
| Method | Endpoint | 설명 |
|---|---|---|
| POST | `/api/admin/inquiries/:id/assign-team` | 관리자가 매칭 실패 건에 팀을 수동 지정 → 정상 플로우(Chat 전송부터) 재진입 |
| GET | `/api/admin/unassigned` | 매칭 실패 후 미배정 상태인 건 목록 (SLA 경과 여부 포함) |

---

## 6. 보안 및 민감정보 (3.3절, 5절 리스크 대응)

- 문의 원문은 팀 전체가 보는 Chat 공간에 그대로 노출되므로, 자동 필터링은 이번 범위에서 제외하되 **서버 로그에도 원문이 과도하게 남지 않도록** 로깅 레벨 조정 (요청 바디 전체를 info 레벨로 찍지 않음)
- 문의자 개인정보(이메일 등)는 최소 수집 원칙 유지
- Google Chat App 인증 정보(서비스 계정 키)는 환경변수/시크릿 매니저로 관리, 저장소에 커밋 금지

---

## 7. 데이터 축적/분석 연동 (4절)

- `inquiries.matched_team_id` + `surveys.match_correct` 조인 쿼리로 1차 정확도 산출 가능하도록 스키마 설계 완료
- 예시 쿼리:
```sql
SELECT
  COUNT(*) FILTER (WHERE s.match_correct) * 1.0 / COUNT(*) AS accuracy
FROM inquiries i
JOIN surveys s ON s.inquiry_id = i.id
WHERE i.match_failed = false AND s.match_correct IS NOT NULL;
```
- `match_audit_log`는 4.2절 편향 보정을 위한 수동 감사 결과 저장용 (별도 관리자 화면에서 랜덤 샘플 감사 시 사용)

---

## 8. 작업 순서 (체크리스트)

1. PostgreSQL 스키마 마이그레이션 작성 (`teams`, `inquiries`, `surveys`, `match_audit_log`)
2. `teams` 시드 데이터 입력 (프론트 mock의 `teams.json`과 동일 키워드로 시작)
3. `CandidateFilter` / `LlmReranker` 인터페이스 정의 + `KeywordCandidateFilter`, `MockLlmReranker` 구현체
4. `POST /api/inquiries` — 등록 + (키워드 후보 추출 → Mock LLM 재판단) + (성공 시) Chat 전송
5. Google Chat App 등록 및 서비스 계정/Webhook 설정 (사내 Workspace 관리자 협업 필요 — 별도 승인 절차 확인)
6. `/api/chat/events` — 리액션 이벤트 수신 → 완료 처리 (`completed_at`만 기록, 설문 자동 발송 없음)
7. `POST /api/inquiries/:id/survey-ready` (관리자 발송 확정), `GET /api/admin/survey-pending` (미발송 목록)
8. `GET /api/inquiries/pending-survey?contact={email}`, `POST /api/inquiries/:id/survey`, `GET /api/inquiries/:id`
9. 무응답 타임아웃 스케줄러 (cron)
10. 프론트 `api.js`의 각 함수를 실제 엔드포인트 fetch 호출로 교체 (연락처 기반 팝업 조회 로직 포함)
11. 1~2개 팀 대상 파일럿 실행 (3.6절), 로그/데이터 모니터링
12. (후속) 사내 구글 SSO 연동 시 `contact` 파라미터를 로그인 세션 이메일로 교체

---

## 9. 이번 단계에서 제외 (문서 상 보류 항목과 동일)

- **실제 LLM API 연동** — PoC 단계는 `MockLlmReranker`로 대체, 사내 보안/비용 검토 후 별도 진행
- 개인 단위 정밀 매칭, 과거 사례 기반 임베딩/RAG 전환 (2단계)
- HR/캘린더 연동 (퇴사자·연차자 처리)
- 민감정보 자동 필터링
- 파인튜닝
